# Dota Lens 0.5.0 玩家报告产品化 PRD

- 文档状态：已完成开发验收，尚未发布
- 目标版本：0.5.0
- 创建日期：2026-07-25
- 关联文档：`DOTA-LENS-PLAYER-ANALYSIS-REPORT-V3-PRD.md`
- 当前协议：`player-report/4.0`

## 1. 文档结论

0.5.0 不新增一个孤立的分析页面，而是在现有“玩家评分”页中保留两个模式：

1. `比赛简报`：默认模式，面向普通玩家。
2. `深度分析`：面向希望核对数据、公式和证据的玩家。

普通模式优先保留以下内容：

- 一句总评。
- 全场所有通过重要性、归责和定位门禁的关键时刻。
- 所有可定位且不重复的明确优点。
- 所有可定位且不重复的主要问题；没有可靠归责时解释为什么不做确定批评。
- 一个下一局训练目标。

深度模式负责证明普通模式的每条结论，完整展示：

- 10 个公共维度。
- 1/2/3/4/5 号位的不同权重和职责目标。
- 基础事实、对位比较和队内职责比较。
- 根因、后果、维度影响和综合分链路。
- 评分中已经计算的部分、额外修正的部分和只作为背景展示的部分。
- 缺失字段、门禁失败和置信度。
- 对应时间、地图、团战、打钱、视野或出装页面跳转。

推荐的最终评分口径为：

```text
基础维度分
+ 未在基础指标中重复计算的行为修正
= 最终维度分

最终综合分
= 所有有效维度的最终维度分 × 位置有效权重
```

必须把行为影响分成三种计分路径：

| 路径 | 含义 | 是否再次加减分 |
| --- | --- | --- |
| `embedded` | 该事件已经进入基础指标，例如职责分已经包含某场团战 | 否，只解释其已有贡献 |
| `modifier` | 该上下文没有进入基础指标，例如关键死亡后 60 秒丢盾 | 是，经过置信度、归责和封顶后修正 |
| `context_only` | 只能确认团队背景，不能归责给当前玩家 | 否，只展示 |

这是 0.5.0 最重要的去重规则。

## 2. 当前实现审计

### 2.1 已经具备

当前代码已经具备以下基础能力：

- 后端输出 `player-report/3.0`。
- 10 个公共维度和五位置权重。
- 普通模式已有一句结论、三个阶段节点、优点、问题和训练计划骨架。
- 4/5 号位不会因为没有抢核心兵线而收到核心打钱建议。
- 战斗职责只读取通过职责门禁的团战、小规模冲突和抓单。
- 同一根因可合并阵亡、装备延误、目标损失和视野缺口。
- 单根因综合负向影响已有 6 分封顶。
- 维度证据可以跳转到对应模块和时间。
- 低置信度维度会从综合分分母移除，而不是按 0 分计算。

### 2.2 仍未闭环

| 问题 | 当前情况 | 0.5.0 目标 |
| --- | --- | --- |
| 普通模式仍像数据看板 | 仍展示五领域分数，重点不够集中 | 普通模式只保留五项核心内容 |
| 关键时刻质量不稳定 | 主要按对线、中期、后期固定阶段生成 | 从统一候选池选出全场所有真正重要且不重复的事件 |
| 同位置比较不可审计 | 同位置对手参与计算，但页面没有解释比较对象和原因 | 每项显示比较对象、原值、差值和选择理由 |
| 根因影响没有进入最终公式 | `attribution_only=true`，主要用于解释 | 建立 `embedded/modifier/context_only` 三路计分 |
| 低分与批评之间关系不清楚 | 只能显示一句“低分不等于明确失误” | 展示低分来源、无法归责原因和建议复核窗口 |
| 位置模板不够完整 | 部分建议按位置区分，阶段叙事仍较通用 | 五位置分别定义职责、问题、优点和训练模板 |
| 回归样本不足 | 本地有 14 个唯一 Replay 文件，位置覆盖尚未整理 | 建立 15 个固定 Replay，五位置各 3 个 |
| 评分漂移不可发现 | 只有单元测试，没有整场报告 Golden 对比 | 固定输入、固定输出摘要和差异审批 |

## 3. 目标与非目标

### 3.1 产品目标

1. 普通玩家在 90 秒内看懂本场最值得保留和最需要改进的内容。
2. 硬核玩家可以从最终分逐层追溯到原始 Replay 事实。
3. 每条确定性批评都必须能回答：
   - 什么时候发生。
   - 在哪里发生。
   - Replay 记录了什么。
   - 为什么判断为问题。
   - 造成了什么后果。
   - 影响了哪些维度。
   - 最终影响了多少分。
   - 下一次应该怎么做。
4. 同一个根因不会因为产生多个后果而重复批评和重复扣分。
5. 不同位置不会收到明显不符合职责的建议。

### 3.2 非目标

0.5.0 暂不承诺：

- 段位百分位。
- 跨比赛长期成长曲线。
- 完全依赖机器学习生成结论。
- 对玩家意图、心态或沟通进行判断。
- 对缺少技能 CD、魔法、距离和合理目标证据的“技能没交”进行确定批评。
- 把预测路线当成实时指挥。

## 4. 方案比较

### 4.1 方案 A：维持解释型根因

基础分保持不变，根因只用于解释。

优点：

- 改动小。
- 不容易重复扣分。
- 评分稳定。

缺点：

- 无法真正完成“根因到综合分”的计算链。
- 用户会看到“影响多个维度”，但综合分没有对应变化。
- 深度分析仍然难以解释某个关键时刻到底影响了多少分。

### 4.2 方案 B：双层评分，推荐

基础指标先形成基础维度分；只有未被基础指标重复计算的上下文才作为行为修正。

优点：

- 能完成完整评分链。
- 可以明确处理重复计算。
- 保留当前 10 维评分的稳定性。
- 能把关键死亡、目标损失和视野背景放到正确位置。

缺点：

- 需要为每个行为影响声明去重模式。
- 需要增加评分审计协议和回归测试。

### 4.3 方案 C：完全事件化评分

所有分数只由逐事件行为组成，不再使用 GPM、XPM、净值、伤害等聚合基础分。

优点：

- 因果链最直观。
- 每一分都能定位到事件。

缺点：

- 当前 Replay 字段不足以覆盖所有职责。
- 没有事件的时间段会被错误理解为没有贡献。
- 对辅助、带线核心和避战阵容不公平。
- 0.5.0 风险和工作量过高。

### 4.4 推荐决策

采用方案 B，并把每条影响明确标记为：

```text
embedded
modifier
context_only
```

## 5. 产品信息架构

### 5.1 页面入口

继续使用现有“玩家评分”Tab，不新增顶级导航。

进入单场比赛后：

```text
玩家评分
├─ 比赛简报，默认
└─ 深度分析
   ├─ 总览
   ├─ 对线
   ├─ 发育与资源
   ├─ 节奏与目标
   ├─ 战斗
   ├─ 视野与团队
   ├─ 可观测执行
   └─ 个人时间线
```

### 5.2 顶部固定信息

两个模式共用紧凑头部：

- 英雄头像和中文英雄名。
- 玩家名。
- 推断位置。
- 角色置信度。
- 胜负。
- 最终综合分和等级，满足显示门槛时显示。
- 主要对位玩家。
- `比赛简报 / 深度分析` 分段切换。

头部不显示：

- 内部协议名。
- 门禁键名。
- 原始模型名。
- 大段技术说明。

### 5.3 响应式布局

| 内容宽度 | 布局 |
| ---: | --- |
| 1440 px 及以上 | 主内容 + 420 至 480 px 证据抽屉 |
| 1024 至 1439 px | 主内容全宽，证据使用覆盖式抽屉 |
| 1024 px 以下 | 单列，证据使用全屏详情层 |

建议字号：

| 元素 | 字号 |
| --- | ---: |
| 一句总评 | 22 至 24 px |
| 模块标题 | 18 px |
| 卡片标题 | 16 至 17 px |
| 正文 | 15 px |
| 关键数字 | 20 至 24 px |
| 辅助信息 | 不低于 13 px |

## 6. 普通模式详细规格

### 6.1 页面内容顺序

```text
一句总评
全场重要时刻
全部可定位且不重复的优点
全部可定位且不重复的问题，或不批评解释
一个训练目标
```

普通模式移除：

- 五领域分数条。
- 10 维评分网格。
- 复杂置信度术语。
- 公式。
- 根因协议名。
- 门禁状态名。

### 6.2 一句总评

一句总评必须包含：

- 当前位置。
- 本场整体职责表现。
- 最主要的正向或负向阶段。
- 下一局最重要的方向。

长度限制：

- 最多 90 个中文字符。
- 不超过两个分号。
- 不使用“模型认为”“系统判断”等措辞。

示例：

```text
这场3号位对线压制明显，但两次先手都早于队友到场；下一局优先把开战时机和队友距离对齐。
```

生成优先级：

```text
主要问题存在
-> 位置 + 最强阶段 + 主要问题 + 训练方向

没有主要问题但有明确优点
-> 位置 + 稳定表现 + 没有确定批评 + 保持方向

事实不足
-> 当前可确认事实 + 无法判断部分 + 推荐复核阶段
```

### 6.3 全场重要时刻

当前固定“对线、中期、后期”节点改为候选事件排序。

候选来源：

- 对线优势或劣势首次明确形成。
- 关键装备完成和完成后的首次行动。
- 高收益或低收益资源窗口。
- 抓单、TP 支援、小规模冲突和团战。
- 关键死亡和买活。
- 肉山、塔、高地或兵营转化。
- 高价值眼位和目标前视野缺口。
- 队伍资源冲突或辅助高价值离线。

重要性计算：

```text
moment_score =
  比赛影响 × 0.30
  + 证据置信度 × 0.25
  + 位置相关度 × 0.20
  + 可行动性 × 0.15
  + 叙事价值 × 0.10
```

所有输入先归一化为 0 至 100。

选择规则：

1. 保留所有通过重要性、归责、定位和行动门禁的事件，不设置三个事件的硬上限。
2. 同一 `root_cause_id` 只能进入一次。
3. 同场战斗的战斗、阵亡和 TP 合并；眼位、目标和肉山使用各自的时间聚类规则。
4. 时间相距少于 45 秒且空间距离少于 900 世界单位时优先合并。
5. 不为了覆盖阶段而加入低价值事实，也不为了凑数量而加入聚合结论。
6. 置信度低于 55 的事件不能成为确定性重要时刻。
7. 未通过个人归责但对理解比赛必要的事实，可以作为 `context_only` 展示。

每个关键时刻固定显示：

| 字段 | 内容 |
| --- | --- |
| 时间与地点 | `25:27 · 肉山区域` |
| 标题 | 不超过 18 个字 |
| 发生了什么 | Replay 事实摘要 |
| 为什么重要 | 对当前位置职责和比赛节奏的影响 |
| 结果 | 阵亡、经济、装备、目标或视野后果 |
| 下次关注 | 一条可执行动作 |
| 两个数字 | 最能证明结论的两个指标 |
| 跳转 | 对应模块和时间 |

点击行为：

- 点击整行：打开证据抽屉。
- 点击跳转图标：直接进入对应模块并同步全局时间。

### 6.4 一个优点

优点必须满足：

- 置信度不低于 70。
- 有事实引用。
- 至少有精确时间范围和地图区域。
- 有可选中的团战、路线、眼位、目标、装备或时间线实体。
- 与当前位置职责相关。
- 不是单纯因为赢了比赛。
- 不是只取最高维度分后拼接一句模板。

优点优先级：

```text
strength_priority =
  正向比赛影响
  × 置信度
  × 位置相关度
  × 可复现程度
```

没有满足条件的优点时显示：

```text
这场还没有足够明确的个人优点。
现有数据不会仅按胜负或最高分自动补写。
```

优点卡不能只写“团战做得好”或“发育节奏稳定”，必须写成：

```text
25:27-25:44 · 肉山区域
这波反手控制做得好

你在敌方先手后 1.8 秒命中两名核心，并保留原力法杖救下1号位。
结果：本方完成两次减员并取得肉山。

[查看这一波] [查看评分依据]
```

点击“查看这一波”后：

- 进入战斗、打钱、视野、发育、出装或时间线模块。
- 时间游标从事件前置时间开始。
- 自动选中对应实体。
- 地图自动聚焦事件地点。
- 显示事件时间范围，而不是只停在一个秒点。
- 返回玩家报告后保留当前玩家和滚动位置。

### 6.5 一个主要问题

主要问题必须来自一个负向根因，而不是最低分维度。

排序：

```text
issue_priority =
  根因加权综合影响
  × 置信度
  × 个人归责强度
  × 可行动性
  × 位置相关度
```

硬条件：

- 置信度不低于 70，55 至 69 只能进入“建议复核”。
- 必须存在事实引用。
- 必须存在可定位到具体一波的跳转目标。
- 至少达到第 16.2 节的 L2 定位等级。
- 必须通过对应模块职责门禁。
- 个人归责不能为 `team_context_only`。
- 同一根因造成的多个后果只能显示为一条问题。

主要问题固定显示：

- 根因标题。
- 时间和地点。
- 一句事实。
- 一句后果汇总。
- 影响维度，例如“影响战斗、生存和目标”。
- “怎么改”。
- “查看这一波”和“查看评分依据”按钮。

问题卡不能只写“注意站位”“提高打钱效率”或“团战要及时到场”，必须写成：

```text
31:08-31:31 · 夜魇上半野区
这波无信息进入野区中断了关键装备

你在两名高威胁敌人消失 9 秒后独自进入野区，并在队友相距 2400
世界单位时阵亡。随后 54 秒内敌方取得二代肉山，你的黑皇杖进度
按本场收入估算延后约 28 秒。

当时更稳妥的选择：先收己方中路线，等待敌方重新出现或队友靠近。

[查看这一波] [查看地图位置] [查看评分依据]
```

“更稳妥的选择”只有在安全性、资源存续、移动时间和队友资源门禁全部通过时显示；否则只展示事实和后果，不虚构最优解。

### 6.6 没有确定批评时

如果维度低于 52，但没有满足主要问题硬条件，问题区域不得为空，也不得凑一条批评。

显示结构：

```text
本场没有可以确定归责的主要问题

该维度的聚合结果偏低，但 Replay 没有足够证据把它归因到某个具体错误。
因此这里只展示低分来源，不判断你当时一定做错了。

建议复核：
18:20 的资源路线
27:40 的目标前站位
```

深度模式同时解释：

- 哪个聚合指标拉低了分数。
- 缺少什么上下文。
- 哪条职责门禁没有通过。
- 为什么没有生成确定性批评。

### 6.7 一个训练目标

训练目标只解决一个问题，固定为：

```text
触发条件
行动
完成标准
```

训练目标优先与主要问题使用相同 `root_cause_id`。

没有主要问题时：

1. 优先选择本场最值得稳定复现的职责动作。
2. 其次选择当前位置的基础训练模板。
3. 不使用与本场事实无关的泛化建议。

示例：

```text
触发条件：准备以3号位先手开战时
行动：先确认至少一名输出队友能在3秒内跟进
完成标准：下一局三次先手中至少两次形成有效控制或减员
```

### 6.8 事件级结论准入

普通模式的明确优点和主要问题必须来自事件级证据。

准入条件：

```text
有开始时间和结束时间
+ 有区域或世界坐标
+ 有实体类型和实体 ID
+ 有事实引用
+ 有对应模块
+ 有可播放的复盘窗口
= 可以进入普通模式
```

只有全场聚合值、没有具体事件时：

- 可以进入深度模式的趋势说明。
- 可以解释维度分。
- 不得成为普通模式的主要优点。
- 不得成为普通模式的主要问题。
- 不得生成“这一波你应该怎样”的确定性建议。

### 6.9 “查看这一波”复盘条

跳转后，在目标模块顶部显示一条临时复盘条：

```text
玩家报告定位
31:08-31:31 · 夜魇上半野区 · 主要问题

[从前10秒播放] [上一条证据] [下一条证据] [返回玩家报告]
```

复盘条同时显示：

- 当前结论标题。
- 当前玩家。
- 根因时间范围。
- 地图区域。
- 关键后果。
- 当前播放时间。
- 返回入口。

播放到窗口结束后自动暂停，不直接继续播放全场。

## 7. 深度模式模块设计

### 7.1 深度总览

首屏展示：

- 最终综合分。
- 基础综合分。
- 行为修正。
- 等级。
- 报告置信度。
- 可评分维度数量。
- 有效权重覆盖。
- 主要对位和同位置对手。
- 10 维评分矩阵。

每个维度卡显示：

- 维度名称。
- 最终分。
- 基础分。
- 行为修正，例如 `-1.8`。
- 位置权重。
- 对综合分贡献。
- 置信度。
- 状态：强项、稳定、需复核、缺失。

点击维度进入对应模块并选中该维度。

### 7.2 对线模块

关联维度：

- 对线执行。

显示：

- 主要对位双方英雄。
- 3/5/7/10 分钟补刀、反补、等级、经验和净值差。
- 线优、均势、线劣形成时间。
- 1/2/3 号位的可复核漏刀与安全经验。
- 4/5 号位的离线时间、离线收益和本路核心安全。
- 对线死亡和塔前压力窗口。
- 对线维度完整公式。
- 正向和负向根因。

跳转：

- 发育复盘地图。
- 对线时间轴。
- 对应死亡或支援片段。

### 7.3 发育与资源模块

关联维度：

- 发育效率。
- 资源决策。

显示：

- 每 5 分钟净值和经验增长。
- 线上、普通野怪、远古和其它来源收入。
- 同位置 GPM、XPM 和净值比较。
- 安全兵线机会、实际收取和可复核漏线。
- 通过门禁的路线建议。
- 线野循环。
- 叠野创造价值和清野收益归属。
- 关键装备时间。
- 20 分钟后的打钱路线关键窗口。
- 路线判断使用的可见敌人、资源状态和移动时间。

跳转：

- 打钱热力图。
- 资源路线地图。
- 出装时间线。
- 指定时间窗口。

### 7.4 节奏与目标模块

关联维度：

- 地图节奏。
- 目标转化。

显示：

- TP 使用和有效响应窗口。
- 神符、智慧符、莲花和关键资源时间点。
- 关键装备完成后的第一轮行动。
- 战斗到塔、肉山、兵营和高地的转化。
- 赢团后未转化窗口。
- 敌方目标事件和当前玩家是否具备到场条件。
- 目标损失与前序根因的时间间隔。

跳转：

- 时间线。
- 目标发生地点。
- 关联团战。
- 关联出装时间。

### 7.5 战斗模块

关联维度：

- 战斗输出。
- 战斗职责。
- 部分生存风险。

显示：

- 当前玩家参与的有效战斗列表。
- 战斗类型、时间、地点和重要性。
- 伤害、伤害占比、击杀转化和存活输出时间。
- 技能、物品、控制、治疗和救援。
- 到场时间和战场存在率。
- 位置职责门禁。
- 每场战斗的职责分。
- 当前战斗对基础维度分的贡献。
- 战斗根因及其阵亡、装备、目标和视野后果。

不显示确定批评的情况：

- 线上消耗。
- 职责门禁未通过。
- 缺少技能 CD、魔法、距离或合理目标。
- 玩家不具备合理到场条件。

跳转：

- 战斗小地图。
- 战斗时间轴。
- 对应技能和物品使用。
- 战前视野。

### 7.6 视野与团队模块

关联维度：

- 视野与团队。
- 部分地图节奏和战斗职责。

显示：

- 当前玩家全部真假眼。
- 放置位置、时间、存活、结束原因和发现次数。
- 眼位评分及评分组成。
- 进攻眼、防守眼、目标眼和保护眼分类。
- 目标前 90 秒视野准备。
- 团战前和战场附近视野。
- 排眼、叠野和团队资源纪律。
- 4/5 号位的个人视野职责。
- 1/2/3 号位看到的团队信息，只作为决策上下文。

跳转：

- 可缩放眼位地图。
- 指定眼位详情。
- 关联团战。
- 对应时间。

### 7.7 可观测执行模块

关联维度：

- 可观测执行。

显示：

- APM 事实。
- 技能和物品使用频率。
- 首次行动延迟。
- 可确认的技能衔接。
- BKB 等关键物品的可用和使用窗口，数据完整时。
- 无效指令率，字段完整后。

重要限制：

- APM 高不等于操作好。
- APM 低不能直接生成批评。
- “技能没交”必须通过技能已学习、CD 完成、魔法足够、未被控制、处于施法距离且存在合理目标等条件。

### 7.8 个人时间线

显示当前玩家的：

- 击杀、死亡和助攻。
- 关键装备。
- TP 和支援。
- 真假眼和排眼。
- 资源路线窗口。
- 团战和职责评分。
- 目标事件。
- 根因和后果。

同一个根因只显示一个主标记，后果在展开后显示。

## 8. 10 维基础评分

### 8.1 公共规则

所有子指标先归一化到 0 至 100。

同位置相对分使用：

```text
relative_score =
clamp(
  50 + 45 × tanh(
    (玩家值 - 对手值)
    / max(1, (|玩家值| + |对手值|) / 2)
  ),
  0,
  100
)
```

“数值越低越好”的指标需要先反向，例如阵亡数：

```text
relative_score(对手阵亡, 玩家阵亡)
```

队内职责占比使用：

```text
share_score =
clamp(
  50 + (玩家队内占比 - 位置目标占比) × 180,
  0,
  100
)
```

维度基础分：

```text
base_dimension_score =
Σ(有效子指标分 × 子指标权重)
/ Σ(有效子指标权重)
```

缺失子指标从分母移除，不按 0 分。

### 8.2 对线执行

#### 1/2/3 号位

```text
对线模型分 = clamp(50 + lane_score × 0.5, 0, 100)

补刀机会转化 =
secured
/ (secured + reviewable_misses)
× 100

基础分 =
对线模型分 × 75%
+ 补刀机会转化 × 25%
```

位置解释：

- 1 号位：补刀、经验、安全接线和辅助离线后的生存。
- 2 号位：补刀、等级、神符窗口和中路主动权。
- 3 号位：自身经验、限制敌方 1 号位和 4 号位离线后的承压。

#### 4/5 号位

```text
辅助路线分 = clamp(50 + support_route.score × 1.2, 0, 100)

基础分 =
对线模型分 × 65%
+ 辅助路线分 × 35%
```

0.5.0 增加展示但暂不强行加权：

- 离线时长。
- 控符、叠野、视野、击杀助攻收益。
- 本路核心离线期间阵亡。
- 核心经验是否因离线扩大劣势。

门禁：

- 对线置信度低于 55 不评分。
- 没有可复核兵线单位时不计算补刀机会转化。
- 辅助离线不能只按时长判错，必须结合收益和核心安全。

### 8.3 发育效率

#### 1/2/3 号位

```text
基础分 =
平均(
  同位置 GPM 相对分,
  同位置 XPM 相对分,
  同位置净值相对分
)
```

至少两个指标有效才评分。

#### 4/5 号位

0.5.0 不把“经济更高”直接理解为“辅助更好”。

支持位发育效率改为：

```text
基础分 =
低资源团队产出 × 50%
+ 同位置经验效率 × 25%
+ 关键功能装节奏 × 25%
```

其中：

- 低资源团队产出由控制、治疗、助攻、视野和叠野价值相对净值计算。
- 同位置经验效率只用于判断是否长期无收益游走，不鼓励抢核心资源。
- 关键功能装节奏只有在 Patch 物品元数据和购买链完整时启用。

如果功能装窗口不可用：

- 该子指标从分母移除。
- 页面显示“缺少按 Patch 的关键功能装窗口”。
- 不因原始 GPM 低生成负面建议。

### 8.4 资源决策

#### 1/2/3 号位

```text
安全兵线转化 × 35%
+ 路线窗口结果 × 50%
+ 线野循环 × 15%
```

路线窗口结果：

```text
route_score =
clamp(
  65
  + matched_windows × 8
  - missed_windows × 8
  - min(24, estimated_loss / 120)
  + min(8, lane_jungle_cycles × 2),
  0,
  100
)
```

仅使用：

- `recommendation_enabled=true`。
- 置信度不低于 70。
- 敌方可见性、资源存续和移动时间门禁通过。

#### 4/5 号位

```text
辅助离线路线 × 65%
+ 叠野团队价值 × 35%
```

不因没有收取安全兵线扣分。

### 8.5 地图节奏

```text
队内节奏贡献占比 × 45%
+ TP/神符同位置相对分 × 25%
+ 有效战斗存在率 × 30%
```

位置目标：

| 位置 | 队内节奏目标占比 | 战斗存在率参考 |
| --- | ---: | ---: |
| 1 | 12% | 55% |
| 2 | 22% | 65% |
| 3 | 20% | 65% |
| 4 | 24% | 72% |
| 5 | 24% | 72% |

限制：

- TP 次数多不自动代表节奏好。
- 神符拾取次数只作为相对事实。
- 关键装备后的激活窗口未完整前，维度证据等级保持 `partial`。

### 8.6 战斗输出

```text
全场队内伤害职责占比 × 55%
+ 有效战斗平均伤害占比 × 25%
+ 击杀转化率 × 20%
```

队内伤害目标：

| 位置 | 目标占比 |
| --- | ---: |
| 1 | 27% |
| 2 | 25% |
| 3 | 19% |
| 4 | 14% |
| 5 | 9% |

限制：

- 只对团战、小规模冲突和抓单中的有效参与者计算战斗片段。
- 线上消耗不进入战斗职责批评。
- 低伤害必须结合英雄职责、存活输出时间和合理目标后才能生成批评。

### 8.7 战斗职责

```text
通过职责门禁的平均职责分 × 75%
+ 队内功能贡献占比 × 25%
```

功能贡献包括：

- 控制。
- 治疗。
- 助攻。
- 视野。
- 叠野价值。
- 承伤与救援，字段可用时。

队内功能目标：

| 位置 | 目标占比 |
| --- | ---: |
| 1 | 11% |
| 2 | 11% |
| 3 | 18% |
| 4 | 30% |
| 5 | 30% |

门禁：

- 至少一场战斗的 `responsibility_gate.status=passed`。
- 职责分低于 55 才进入问题候选。
- 职责分不通过门禁时只显示事实，不显示分数和批评。

### 8.8 生存风险

```text
同位置阵亡相对分 × 65%
+ 死亡时间分 × 35%

死亡时间分 =
clamp(88 - 死亡时间占比 × 2.2, 0, 100)
```

行为上下文用于解释：

- 是否为关键死亡。
- 是否携带未使用买活。
- 是否处于目标前窗口。
- 是否有可见敌方威胁。
- 是否有合理撤退路径。

阵亡数量已经进入基础分，因此具体阵亡默认使用 `embedded`，不得再次因为“发生阵亡”重复扣分。只有尚未进入基础指标的目标前风险上下文可以作为 `modifier`。

### 8.9 目标转化

```text
队内建筑伤害职责占比 × 75%
+ 塔/肉山直接结果 × 25%
```

建筑伤害目标：

| 位置 | 目标占比 |
| --- | ---: |
| 1 | 30% |
| 2 | 22% |
| 3 | 24% |
| 4 | 14% |
| 5 | 10% |

当前直接结果：

```text
clamp(
  50
  + tower_kills × 12
  + roshan_kills × 14,
  0,
  100
)
```

0.5.0 行为修正补充：

- 赢团后 90 秒内未转化。
- 关键死亡后敌方取得肉山、塔、兵营或高地。
- 目标前兵线和到场条件。

因果证据不足时使用 `context_only`。

### 8.10 视野与团队

视野活动值：

```text
vision_value =
vision_score
+ observer_wards × 15
+ sentry_wards × 8
+ dewards × 25
```

基础分：

```text
队内视野职责占比 × 65%
+ 个人眼位平均评分 × 35%
```

队内视野目标：

| 位置 | 目标占比 |
| --- | ---: |
| 1 | 4% |
| 2 | 6% |
| 3 | 8% |
| 4 | 34% |
| 5 | 42% |

门禁：

- 必须存在眼位生命周期模块。
- 发现事件缺失时降低置信度。
- 战前视野缺口对 1/2/3 号位默认 `context_only`。
- 4/5 号位只有在重要战斗、合理准备时间和可购买/可放置条件成立时才允许个人或共享职责修正。

### 8.11 可观测执行

```text
action_continuity =
clamp(
  40 + max(0, APM - 60) × 0.2,
  30,
  85
)

基础分 =
action_continuity × 60%
+ 同位置技能/物品使用频率相对分 × 40%
```

该维度只描述 Replay 可观察到的执行活跃度，不等价于操作水平。

限制：

- 不根据低 APM 生成确定批评。
- 不评价镜头移动，Replay 当前缺少可靠镜头数据。
- 不评价无效指令率，字段未建立前保持缺失。
- 英雄连招需要按 Patch 技能元数据和机会门禁。

## 9. 五位置职责和建议模板

### 9.1 1 号位

优先职责：

- 对线补刀和经验。
- 安全兵线与野区循环。
- 关键装备窗口。
- 持续输出和生存。
- 赢团后的目标转化。

问题模板优先级：

1. 可确认漏线或低收益路线。
2. 无信息处理危险线导致关键死亡。
3. 关键装备完成后长期未形成资源或战斗转化。
4. 有合理输出条件但持续输出不足。

训练模板：

```text
触发：准备处理越河兵线时
行动：先确认至少两名高威胁敌人的位置和最近消失时间
完成：没有信息时改收己方半区资源，连续两个窗口不发生无信息死亡
```

### 9.2 2 号位

优先职责：

- 对线等级与神符。
- 第一件节奏装后的地图行动。
- 清线和支援机会成本。
- 爆发、控制和目标选择。

问题模板优先级：

1. 等级或神符窗口丢失。
2. 清线后空转。
3. 关键装备完成后未激活。
4. 爆发技能分散到无法减员的目标。

训练模板：

```text
触发：清完中路且下一轮神符少于45秒
行动：保留移动和技能资源，提前向一侧符点靠近
完成：神符刷新时已经占据一侧河道或明确换取边路收益
```

### 9.3 3 号位

优先职责：

- 限制敌方 1 号位。
- 4 号位离线后的经验和生存。
- 先手装和团队装窗口。
- 开战、承伤和空间创造。
- 危险线压力。

问题模板优先级：

1. 没有队友跟进条件的先手。
2. 4 号位离线后无意义越线换血。
3. 先手装完成后没有形成地图压力。
4. 承伤没有换取技能、减员或空间。

训练模板：

```text
触发：准备先手开战时
行动：确认至少一名输出队友能在3秒内跟进
完成：三次先手中至少两次形成有效控制、减员或明确逼退
```

### 9.4 4 号位

优先职责：

- 对线换血和有收益游走。
- 控符、叠野、TP 支援和烟雾。
- 先手或反手控制。
- 功能物品。
- 目标前视野。

问题模板优先级：

1. 长时间离线但没有收益，并使 3 号位失去经验或阵亡。
2. 关键冲突迟到。
3. 有合理条件但第一轮控制或救援未完成。
4. 目标前缺少准备视野。

训练模板：

```text
触发：准备离开3号位超过20秒
行动：先确认兵线、3号位安全和明确收益目标
完成：离线期间3号位不阵亡，且长离线至少产生控符、叠野、视野或有效支援之一
```

### 9.5 5 号位

优先职责：

- 保护 1 号位。
- 拉野、补给和经验分配。
- 真假眼、排眼和目标准备。
- 救人和反手。
- 低资源团队价值。

问题模板优先级：

1. 离开 1 号位时没有处理兵线、补给和退路。
2. 目标前没有形成反隐和入口视野。
3. 核心受到先手时未完成可用的救援或反手。
4. 死亡没有换取信息、技能或目标。

训练模板：

```text
触发：准备离开1号位拉野、插眼或支援
行动：确认兵线位置、核心补给和安全退路
完成：动作期间1号位不阵亡，完成后及时回到可支援距离
```

## 10. 根因、后果与去重

### 10.1 根因结构

每条根因包含：

- 稳定 `root_cause_id`。
- 当前玩家。
- 时间范围。
- 地点。
- 根因标题。
- 主事实。
- 判断。
- 置信度。
- 个人归责类型。
- 证据引用。
- 跳转目标。
- 后果列表。
- 维度影响列表。

### 10.2 可合并后果

| 后果 | 证据状态 | 关联规则 |
| --- | --- | --- |
| 阵亡 | `fact` | 根因窗口内的当前玩家死亡 |
| 金钱损失 | `fact` | 与死亡时间相差不超过 2 秒的死亡损失 |
| 关键装备延误 | `estimated` | 死亡损失金钱除以本场 GPM，并关联下一件关键成装 |
| 目标损失 | `derived` | 根因开始后 90 秒内敌方取得关键目标 |
| 视野缺口 | `gated` | 重要战斗前和附近均无可确认假眼，且交换失利或丢目标 |
| 买活后果 | `derived` | 买活、死亡和目标窗口数据完整后启用 |

### 10.3 合并规则

满足以下条件时合并为同一根因：

1. 当前玩家相同。
2. 主事实引用相同，或事件属于同一场战斗。
3. 时间窗口重叠，或后果落在规定关联窗口内。
4. 空间位置一致，或属于同一目标区域。
5. 没有出现新的独立决策点。

不得合并：

- 相隔较远的两次独立先手。
- 同时发生在两个地点的冲突。
- 中间有明确脱战和重新决策。
- 只有结果相似，但事实来源不同。

### 10.4 归责类型

| 类型 | 含义 | 计分 |
| --- | --- | --- |
| `personal` | 当前玩家具备明确职责和合理行动条件 | 100% |
| `shared` | 多名玩家共同负责 | 50% |
| `team_context` | 只能确认团队背景 | 0%，只展示 |
| `unknown` | 缺少关键字段 | 0%，抑制批评 |

## 11. 完整评分链

### 11.1 第一步：基础子指标

每个子指标保存：

```json
{
  "key": "fight_score",
  "value": 42,
  "counterpart": 58,
  "normalized_score": 42,
  "local_weight": 75,
  "confidence": 88,
  "evidence_refs": ["combat:fight-34:3"]
}
```

### 11.2 第二步：基础维度分

```text
base_score =
Σ(normalized_score × local_weight)
/ Σ(local_weight)
```

页面必须显示每个子指标对基础分的贡献。

### 11.3 第三步：根因影响

每个根因对每个维度输出：

```json
{
  "dimension": "objective_conversion",
  "raw_delta": -3.0,
  "score_path": "modifier",
  "overlap_reason": "enemy_objective_loss_not_in_base_metric",
  "confidence_factor": 0.88,
  "responsibility_factor": 0.5,
  "applied_delta": -1.32
}
```

计算：

```text
candidate_delta =
raw_delta
× confidence_factor
× responsibility_factor
× overlap_factor
```

建议系数：

| 项目 | 系数 |
| --- | ---: |
| 置信度 | `confidence / 100` |
| 个人归责 | 1.0 |
| 共享归责 | 0.5 |
| 团队背景 | 0 |
| 已包含于基础分 | overlap 0 |
| 部分重叠 | overlap 0.5 |
| 独立上下文 | overlap 1.0 |

### 11.4 第四步：封顶

封顶顺序：

1. 单根因对单维度原始影响限制在 `-25 至 +25`。
2. 单根因加权后的综合负向影响最多 `-6` 分。
3. 单场所有行为修正对单维度最多 `-15 至 +10`。
4. 单场所有行为修正对综合分最多 `-12 至 +8`。

正向影响不抵消同根因的负向封顶计算，但会在最终维度分中独立展示。

### 11.5 第五步：最终维度分

```text
final_dimension_score =
clamp(
  base_dimension_score
  + Σ(applied_modifier_delta),
  0,
  100
)
```

`embedded` 事件的影响已经包含在 `base_dimension_score` 中，页面显示“已计入基础分”，不能再次相加。

### 11.6 第六步：有效位置权重

原始位置权重见第 12 节。

缺失维度从分母移除：

```text
effective_weight_d =
role_weight_d
/ Σ(所有有效维度 role_weight)
```

### 11.7 第七步：综合分

```text
base_overall =
Σ(base_dimension_score × effective_weight)

final_overall =
Σ(final_dimension_score × effective_weight)

behavior_adjustment =
final_overall - base_overall
```

等级：

| 分数 | 等级 |
| ---: | --- |
| 88 至 100 | S |
| 78 至 87 | A |
| 68 至 77 | B |
| 58 至 67 | C |
| 45 至 57 | D |
| 0 至 44 | E |

角色置信度低于 65 或有效权重不足 60 时：

- 可以展示已有维度事实。
- 不展示综合等级。
- 明确说明原因。

## 12. 五位置权重

| 位置 | 对线 | 发育 | 资源 | 节奏 | 输出 | 职责 | 生存 | 目标 | 视野 | 执行 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 号位 | 15 | 20 | 15 | 5 | 15 | 8 | 10 | 7 | 2 | 3 |
| 2 号位 | 17 | 12 | 8 | 15 | 14 | 10 | 8 | 7 | 3 | 6 |
| 3 号位 | 15 | 8 | 7 | 15 | 8 | 20 | 10 | 8 | 5 | 4 |
| 4 号位 | 12 | 3 | 5 | 20 | 5 | 20 | 7 | 5 | 16 | 7 |
| 5 号位 | 15 | 2 | 4 | 15 | 3 | 18 | 7 | 5 | 23 | 8 |

每行合计 100。

## 13. 分数证据抽屉

### 13.1 综合分抽屉

显示：

```text
基础综合分 66.4
行为修正 -2.1
最终综合分 64.3
```

下方按维度列出：

| 维度 | 基础分 | 修正 | 最终分 | 有效权重 | 综合贡献 |
| --- | ---: | ---: | ---: | ---: | ---: |

### 13.2 维度抽屉

显示顺序：

1. 最终分、基础分和行为修正。
2. 位置权重和有效权重。
3. 比较对象。
4. 基础子指标。
5. 行为根因。
6. 缺失字段和门禁。
7. 评分公式。

### 13.3 根因抽屉

固定流程：

```text
Replay 事实
↓
程序判断
↓
根因
↓
后果
↓
维度影响
↓
综合分路径
↓
下一次动作
```

每个维度影响必须显示：

- 原始影响。
- 置信度系数。
- 归责系数。
- 去重类型。
- 封顶前后。
- 实际应用分值。

## 14. 比较对象

### 14.1 比较类型

| 维度 | 默认比较 |
| --- | --- |
| 对线执行 | 实际主要对位 |
| 发育效率 | 敌方同位置 |
| 资源决策 | 自身机会窗口和位置规则 |
| 地图节奏 | 敌方同位置 + 本队职责占比 |
| 战斗输出 | 本队职责目标 + 敌方同位置 |
| 战斗职责 | 位置职责目标 |
| 生存风险 | 敌方同位置 |
| 目标转化 | 本队职责目标 |
| 视野与团队 | 本队职责目标 |
| 可观测执行 | 敌方同位置，只作相对事实 |

### 14.2 页面必须解释

不能只显示“对位 58”。

必须显示：

```text
比较对象：敌方3号位
选择原因：相同位置职责
你的数值：510 GPM
对方数值：472 GPM
差值：+38
归一化分：54
```

如果位置识别置信度不足：

- 不使用同位置比较。
- 降级为队内职责或自身阶段比较。
- 页面说明降级原因。

## 15. 数据协议

建议升级为 `player-report/4.0`，避免旧前端误解新评分语义。

### 15.1 顶层结构

```json
{
  "model": "player-report/4.0",
  "formula_version": "player-score/2.0",
  "slot": 3,
  "position": 4,
  "role_confidence": 92,
  "brief": {},
  "score_card": {},
  "dimensions": [],
  "story_nodes": [],
  "insights": [],
  "root_causes": [],
  "training_plan": [],
  "evidence_index": {},
  "caveats": []
}
```

### 15.2 `brief`

```json
{
  "verdict": {
    "text": "本场一句总评",
    "tone": "review",
    "confidence": 82
  },
  "key_moment_ids": ["moment:1", "moment:2", "moment:3"],
  "strength_id": "insight:strength:1",
  "primary_issue_id": "root:combat:34:3",
  "no_issue_explanation": null,
  "training_goal_id": "training:root:combat:34:3"
}
```

### 15.3 `score_card`

```json
{
  "base_overall": 66.4,
  "behavior_adjustment": -2.1,
  "final_overall": 64.3,
  "grade": "C",
  "confidence": 81,
  "dimension_coverage": 9,
  "available_weight": 95,
  "formula": "weighted-valid-dimensions/2.0"
}
```

### 15.4 `dimensions[]`

```json
{
  "key": "combat_duty",
  "base_score": 58,
  "behavior_delta": -1.2,
  "final_score": 56.8,
  "role_weight": 20,
  "effective_weight": 21.1,
  "weighted_contribution": 12.0,
  "confidence": 86,
  "status": "stable",
  "comparator": {},
  "base_components": [],
  "behavior_components": [],
  "missing": []
}
```

### 15.5 `root_causes[]`

```json
{
  "id": "root:combat:fight-34:3",
  "title": "先手早于队友到场",
  "time_start": 1527,
  "time_end": 1554,
  "location": "肉山区域",
  "confidence": 88,
  "responsibility": "personal",
  "primary_fact_refs": ["combat:fight-34:3"],
  "consequences": [],
  "dimension_impacts": [],
  "jump_target": {
    "module": "combat",
    "entity_type": "fight",
    "entity_id": "fight-34",
    "time": 1527,
    "range_start": 1517,
    "range_end": 1559,
    "player_slot": 3,
    "location_level": "L3",
    "map_focus": {
      "world_x": 1234,
      "world_y": 5678,
      "region": "roshan_pit"
    }
  }
}
```

### 15.6 事件级优点和问题

普通模式引用的优点和问题必须输出：

```json
{
  "id": "insight:fight-34:3",
  "kind": "improvement",
  "root_cause_id": "root:combat:fight-34:3",
  "title": "这波先手早于队友到场",
  "what_happened": "你先手时最近输出队友相距2400世界单位。",
  "why_it_matters": "队友无法在控制时间内完成跟进。",
  "result": "本方先手技能进入冷却，随后交换失利。",
  "next_action": "先确认一名输出队友能在3秒内跟进。",
  "confidence": 88,
  "evidence_refs": ["combat:fight-34:3"],
  "jump_target": {
    "module": "combat",
    "entity_type": "fight",
    "entity_id": "fight-34",
    "time": 1527,
    "range_start": 1517,
    "range_end": 1559,
    "player_slot": 3,
    "location_level": "L3"
  }
}
```

以下字段缺少任意一项时，不能成为普通模式主要优点或主要问题：

- `what_happened`。
- `why_it_matters`。
- `result`。
- `evidence_refs`。
- `jump_target.range_start/range_end`。
- `jump_target.entity_type/entity_id`。
- L2 或 L3 定位。

## 16. 跨模块跳转

### 16.1 当前问题

当前玩家报告的跳转主要只有：

```text
module + time + player_slot
```

这只能把用户带到大概页面和时间，不能保证：

- 自动选中对应团战。
- 自动选中对应路线窗口。
- 自动选中对应眼位或装备。
- 地图聚焦真实发生地点。
- 从事件发生前开始回看。
- 播放到事件结束自动停下。

0.5.0 必须把跳转升级为“复盘定位协议”。

### 16.2 定位等级

| 等级 | 数据 | 普通模式用途 |
| --- | --- | --- |
| L3 精确定位 | 开始/结束时间、世界坐标、实体 ID、模块 | 可作为明确优点或问题 |
| L2 区域定位 | 开始/结束时间、地图区域、实体 ID、模块 | 可作为明确优点或问题 |
| L1 时间定位 | 单个时间或阶段，没有可靠区域和实体 | 只能作为阶段事实 |
| L0 聚合事实 | 只有全场数值 | 只能进入深度评分解释 |

普通模式的优点和主要问题至少需要 L2。

### 16.3 统一跳转结构

```json
{
  "module": "combat",
  "entity_type": "fight",
  "entity_id": "fight-34",
  "player_slot": 3,
  "time": 1527,
  "range_start": 1517,
  "range_end": 1559,
  "autoplay_start": 1517,
  "autoplay_stop": 1559,
  "location_level": "L3",
  "map_focus": {
    "world_x": 1234,
    "world_y": 5678,
    "region": "roshan_pit",
    "zoom": 1.8,
    "coordinate_patch": "7.41",
    "transform_version": "map-coordinate/2.0",
    "confidence": 91
  },
  "evidence_refs": ["combat:fight-34:3"],
  "return_context": {
    "view": "player-score",
    "mode": "brief",
    "player_slot": 3,
    "evidence_id": "root:combat:fight-34:3"
  }
}
```

字段要求：

- `time`：主定位时间。
- `range_start/range_end`：这波需要复盘的完整范围。
- `autoplay_start/autoplay_stop`：自动播放范围。
- `entity_type/entity_id`：进入页面后要自动选中的对象。
- `map_focus`：后端统一世界坐标和地图校准信息。
- `return_context`：返回报告时恢复原位置。

### 16.4 不同事件的复盘窗口

| 事件 | 开始 | 结束 |
| --- | --- | --- |
| 团战/小规模冲突 | 首次有效接触前 10 秒 | 最后有效接触后 5 秒 |
| 抓单 | 先手前 8 秒 | 击杀或脱离后 5 秒 |
| 关键死亡 | 死亡前 10 秒 | 死亡后 15 秒 |
| 对线问题 | 当前兵线进入接触前 10 秒 | 该波兵线结束或事件后 20 秒 |
| 打钱路线 | 路线判断窗口前 10 秒 | 路线窗口结束后 5 秒 |
| 眼位优点/问题 | 放置前 5 秒 | 首次关键发现、被拆或目标结束 |
| 关键装备激活 | 成装前 5 秒 | 成装后第一轮行动结束 |
| 目标转化 | 前序战斗结束前 5 秒 | 目标取得或窗口关闭后 10 秒 |

如果根因有多个后果：

- 主按钮“查看这一波”跳到根因开始。
- 每个后果提供自己的次级定位。
- 例如阵亡、肉山损失和装备完成时间分别可以点击。

### 16.5 模块与实体映射

| 结论类型 | 模块 | 实体类型 | 进入后动作 |
| --- | --- | --- | --- |
| 对线 | `development` | `lane_checkpoint` 或 `lane_wave` | 选中检查点或兵线并显示双方位置 |
| 打钱路线 | `farm` | `farm_diagnostic` | 选中路线窗口并显示实际/建议路线 |
| 团战职责 | `combat` | `fight` | 选中战斗并将左侧列表滚动居中 |
| 眼位 | `vision` | `ward` | 选中眼位、放大地图并显示生命周期 |
| 装备完成 | `build` | `purchase` | 选中装备并定位购买时间 |
| 目标转化 | `combat` 或 `timeline` | `objective` | 选中目标及其前序战斗 |
| 阵亡 | `timeline` | `death` | 选中死亡并显示死亡前后位置 |
| TP 支援 | `timeline` 或 `combat` | `teleport` | 选中 TP 起点、落点和到场时间 |

### 16.6 地图定位

地图定位必须使用 Parser 输出的统一世界坐标和 `MapCoordinateService` 校准结果。

禁止：

- 前端根据百分比重新猜坐标。
- 不同页面各用一套缩放公式。
- 地图放大后重新计算事件坐标。
- 使用固定 Patch 地图转换所有比赛。

要求：

- 世界坐标是唯一事实源。
- 前端只应用统一变换矩阵。
- 缩放和平移只改变视口，不改变标记相对地图的位置。
- `coordinate_patch` 和 `transform_version` 随跳转保存。
- 坐标置信度不足时降级到 L2 区域定位，不显示虚假的精确点。

地图焦点显示：

- 当前玩家位置。
- 关键敌方最后可见位置，数据允许时。
- 队友距离。
- 事件中心点。
- 实际路线和建议路线，打钱事件适用。
- 视野范围，眼位和战斗事件适用。

### 16.7 跳转执行顺序

```text
保存玩家报告返回上下文
-> 加载目标模块
-> 设置当前玩家
-> 设置复盘开始时间
-> 选中实体 ID
-> 应用地图焦点
-> 显示复盘条
-> 用户点击后开始播放
-> 到 range_end 自动暂停
```

默认不在跳转后立即自动播放，避免用户还没看清页面就错过事件。用户点击“从前10秒播放”后才开始。

### 16.8 返回行为

点击“返回玩家报告”后恢复：

- 原玩家。
- 比赛简报或深度分析模式。
- 原维度。
- 原证据 ID。
- 原滚动位置。
- 证据抽屉打开状态。

### 16.9 跳转验收

- 时间误差不超过 1 秒。
- L3 地图点必须使用同一世界坐标，在缩放和平移前后相对位置不变。
- 实体 ID 自动选中成功率 100%。
- 战斗列表自动将目标战斗滚动到可视区域中部。
- 打钱页面自动显示该窗口的实际路线和建议路线。
- 视野页面自动选中眼位并显示其放置、结束、发现和评分。
- 复盘窗口播放到结束时间后自动暂停。
- 返回玩家报告时保留玩家、模式、维度和滚动位置。
- 15 场固定 Replay 中所有普通模式优点和问题的跳转成功率 100%。

## 17. 低分但没有批评

### 17.1 触发条件

满足以下任一条件：

- 最终维度分低于 52。
- 综合分低于 58。

同时：

- 没有置信度不低于 70 的个人或共享负向根因。
- 或所有负向候选都因门禁、上下文或归责失败被抑制。

### 17.2 数据结构

```json
{
  "type": "low_score_without_attribution",
  "dimension": "farm_efficiency",
  "aggregate_reason": "同位置GPM和净值偏低",
  "why_no_criticism": [
    "缺少逐波安全兵线状态",
    "无法确认是否因合理参战放弃资源"
  ],
  "review_windows": [
    {
      "time": 1100,
      "module": "farm",
      "label": "18:20 资源路线"
    }
  ]
}
```

### 17.3 文案原则

必须区分：

- `结果偏低`。
- `可以确认的错误`。

禁止：

```text
你的发育很差，所以路线选择错误。
```

允许：

```text
本场同位置经济结果偏低，但现有数据无法确认是路线错误、合理参战还是资源让渡造成，因此不生成确定批评。
```

## 18. 15 场固定 Replay 回归

### 18.1 当前资产缺口

当前工作区已确认 16 个唯一 Replay，其中 15 场进入正式矩阵，1 场作为候补。

正式矩阵同时执行两层验证：

- 全员协议体检：15 场全部 10 人，共 150 份 `player-report/4.0`。
- Golden 内容精查：固定 15 名重点玩家，1/2/3/4/5 号位各 3 份。

2026-07-28 验收结果为 15/15 场、150/150 份协议、15/15 份 Golden 全部通过；五位置各覆盖 30 份全员报告。

### 18.2 样本矩阵

| 位置 | 数量 | 最低覆盖 |
| --- | ---: | --- |
| 1 号位 | 3 | 一场对线优、一场路线问题、一场关键死亡或输出问题 |
| 2 号位 | 3 | 一场神符/等级、一场装备激活、一场支援或目标问题 |
| 3 号位 | 3 | 一场压制成功、一场先手问题、一场承压或空间问题 |
| 4 号位 | 3 | 一场游走收益、一场战斗职责、一场视野或叠野 |
| 5 号位 | 3 | 一场对线保护、一场目标视野、一场救援或反手 |

额外覆盖：

- 胜利和失败。
- 20 分钟前结束和 40 分钟以上。
- 至少一场数据不完整。
- 至少一场没有高置信度主要问题。
- 至少一场同根因产生四类后果。
- 至少一场位置推断置信度不足。

### 18.3 Fixture 结构

不把超大 Replay 输出直接复制到前端测试。

建议：

```text
tests/player-report-regression/
├─ manifest.json
├─ expected/
│  ├─ <match_id>-slot-<slot>.json
│  └─ ...
└─ README.md
```

`manifest.json` 保存：

- Match ID。
- Replay SHA-256。
- 测试玩家槽位。
- 期望位置。
- Patch。
- 必测模块。
- 已知缺失字段。
- 人工审核标签。

### 18.4 自动回归

每个样本必须验证：

1. 输出 `player-report/4.0`。
2. 10 个维度键完整且顺序稳定。
3. 五位置原始权重合计 100。
4. 缺失维度不输出分数。
5. 综合分可以从维度精确重算。
6. 普通模式输出一句总评、全部通过门禁的重要时刻、全部不重复的可定位优缺点和一个训练目标。
7. 同一 `root_cause_id` 在普通模式只出现一次。
8. 所有确定性负面结论有事实引用。
9. 所有普通模式优点和问题都有 L2/L3 定位、复盘范围和实体 ID。
10. 单根因综合扣分不超过 6。
11. `embedded` 影响不会再次加减分。
12. `context_only` 不进入最终分。
13. 没有高置信度问题时生成不批评解释。
14. 普通模式不出现内部协议、门禁键或英文内部枚举。
15. 英雄、装备和技能优先显示中文名。
16. “查看这一波”进入正确模块并自动选中对应实体。
17. 地图缩放和平移前后事件标记位置不漂移。
18. 返回玩家报告后上下文完整恢复。

### 18.5 Golden 差异

每次评分公式变化输出：

- 综合分变化。
- 每个维度变化。
- 根因数量变化。
- 主要问题变化。
- 重要时刻集合变化。
- 训练目标变化。

默认失败条件：

- 综合分漂移超过 3 分且没有批准记录。
- 主要问题从有证据变为无证据。
- 跳转目标丢失。
- 同一根因重复出现。
- 位置模板串位。

### 18.6 人工内容 QA

每场至少审核一个指定玩家，按以下问题打勾：

- 90 秒内能否看懂。
- 一句总评是否与数据一致。
- 所有被选中的时刻是否都足够重要，是否存在遗漏或重复。
- 优点是否具体。
- 问题是否可归责。
- 建议是否符合位置职责。
- 训练目标是否可执行和可检查。
- 是否存在重复批评。
- 是否存在过度确定的措辞。

## 19. 前端测试

至少新增以下测试：

1. 普通模式不再展示五领域分数。
2. 普通模式完整保留所有通过门禁的优缺点。
3. 全场重要时刻按重要性、聚类和去重规则选择。
4. 训练目标与主要问题根因一致。
5. 没有主要问题时显示不批评解释。
6. 1/2/3/4/5 号位模板不会串位。
7. 综合分抽屉可重算。
8. `embedded` 不重复加减分。
9. `context_only` 不进入综合分。
10. 根因后果链完整展示。
11. 优点“查看这一波”跳到正确模块、范围和实体。
12. 问题“查看这一波”跳到正确模块、范围和实体。
13. 团战跳转后目标战斗在列表中居中。
14. 打钱跳转后目标路线窗口被选中。
15. 眼位跳转后地图自动聚焦并选中目标眼位。
16. 复盘范围播放结束后自动暂停。
17. 返回报告后保留状态和滚动位置。
18. 普通模式不出现内部术语。
19. 1024×720 无横向溢出和文字裁切。
20. 英雄头像、装备和技能图片有本地回退。

## 20. 后端测试

至少新增：

- `PlayerReportScoreFormulaV4Test`。
- `PlayerReportRootCauseDedupeTest`。
- `PlayerReportRoleTemplateTest`。
- `PlayerReportBriefSelectionTest`。
- `PlayerReportLowScoreExplanationTest`。
- `PlayerReportJumpTargetTest`。
- `PlayerReportLocalizationEligibilityTest`。
- `PlayerReportReplayRegressionTest`。

关键断言：

```text
final_dimension = base_dimension + applied_modifiers
final_overall = Σ(final_dimension × effective_weight)
```

允许浮点误差不超过 `0.05`。

## 21. 实施阶段

### 阶段 A：评分协议

- 新增 `player-report/4.0`。
- 输出基础分、修正分和最终分。
- 增加三种计分路径。
- 增加比较对象说明。
- 增加低分不归责结构。

完成标准：

- 单元测试可完整重算综合分。
- 同根因不会重复扣分。

### 阶段 B：普通模式

- 移除五领域分数。
- 实现统一候选池和全场重要时刻选择器。
- 保留所有可定位且不重复的优缺点，并收敛为一个训练目标。
- 增加无批评解释。
- 五位置文案模板全部接入。

完成标准：

- 普通玩家 90 秒可理解。
- 页面不出现内部术语。
- 全部优缺点都能定位并进入有界复盘。

### 阶段 C：深度分析

- 综合分审计面板。
- 10 维基础与修正拆解。
- 比较对象和选择原因。
- 根因后果链。
- 缺失字段和门禁。

完成标准：

- 任意负面结论可以追溯到事实和最终分。

### 阶段 D：跨模块跳转

- 将当前“模块 + 时间”升级为完整复盘定位协议。
- 统一 `jump_target` 的时间范围、实体 ID、地图焦点和返回上下文。
- 增加 L0 至 L3 定位等级。
- 普通模式优点和问题只接收 L2/L3 事件。
- 增加“查看这一波”复盘条和范围播放。
- 团战、路线、眼位、装备、目标和死亡支持实体自动选中。
- 返回报告保持上下文。

完成标准：

- 15 场样本的优点、问题和建议跳转成功率 100%。
- 所有普通模式明确结论都能定位到具体一波。

### 阶段 E：15 场回归

- 补齐 15 个固定 Replay。
- 建立五位置样本矩阵。
- 建立 Golden 输出和差异审批。
- 完成人工内容 QA。

完成标准：

- 五个位置各 3 场通过。
- 无证据确定性批评为 0。

## 22. 0.5.0 发布门槛

必须全部满足：

- 普通模式严格符合五项内容约束。
- 每个位置至少 3 场固定 Replay。
- 所有确定性负面结论具备事实、时间、地点和跳转。
- 所有普通模式明确优点同样具备事实、时间、地点和跳转。
- 普通模式不存在只有泛化文案、无法定位到具体一波的优点或问题。
- 每个“查看这一波”都能选中具体实体并展示完整复盘范围。
- 同一根因在普通模式只出现一次。
- 同一结果不会在基础分和行为修正中重复计算。
- 单根因综合负向影响不超过 6 分。
- 低分但无归责时有明确解释。
- 缺失数据不按 0 分。
- 五位置建议模板回归无串位。
- 1024×720、1280×720、1460×920 和 1920×1080 布局通过。
- 前端控制台无错误。
- Parser 全量测试通过。
- EXE 安装、启动、解析、冷重启和卸载冒烟通过。

## 23. 需要产品确认的决策

实施前需要确认：

1. 是否采用推荐的双层评分和三种计分路径。
2. 普通模式是否彻底移除五领域分数，只在深度模式显示。
3. 行为修正的全场封顶是否采用负向 12 分、正向 8 分。
4. 支持位“发育效率”是否按低资源团队产出重新定义。
5. 15 场是否必须是 15 个不同 Replay，本文按 15 个不同 Replay 设计。

## 24. 推荐实施顺序

建议顺序：

1. 先实现评分协议和可重算测试。
2. 再实现普通模式内容选择。
3. 接着实现深度评分审计面板。
4. 然后补齐跨模块精确跳转。
5. 最后用 15 场 Replay 做五位置回归和内容 QA。

不建议先改视觉样式再补评分协议，否则普通模式和深度模式会继续依赖不稳定的数据结构。

## 25. 2026-07-25 实施记录

本轮完成阶段 D 的第一条可发布纵向链路，但未宣称整个阶段 D 或 0.5.0 发布门槛完成。

### 25.1 已实施

- Parser 新增 `player-report-localization/1.0`，覆盖对线检查点、打钱诊断和战斗片段。
- `jump_target` 已包含实体、复盘范围、L0-L3、地图焦点和返回上下文。
- 聚合优点固定为 L0 且 `ordinary_eligible=false`。
- 前端对 Parser 字段执行第二次严格校验，越界地图坐标、缺实体、缺范围和旧包结论不会进入普通模式明确优缺点。
- 普通模式卡片增加时间范围、地点、事实、影响、行动、“查看这一波”和“查看评分依据”。
- 点击可选择团战、打钱诊断和对线检查点；前端同时具备眼位、购买、死亡、目标和 TP 的导航映射。
- 新增复盘条、从头播放、区间终点自动暂停及返回报告上下文恢复。

### 25.2 真实样本结果

`8894766243` 使用本地 Replay 强制重解析：

- 83.5 MB Replay，267,683 条事件，总耗时 46.6 秒。
- 10 名玩家全部写入定位协议，每人 1 至 3 条普通模式可定位结论。
- 洞察定位等级合计：L3 14 条、L2 8 条、L1 4 条、L0 0 条。
- `fight-63` 能从优点卡跳到团战页、选中实体、居中列表并显示 `39:21-40:44` 复盘范围。
- `lane:5:600` 能从问题卡跳到发育复盘，显示 `09:40-10:20` 和下路区域。
- 4 倍速在区间终点后 17 ms 内自动暂停。
- 1440×900 和 1024×768 无复盘条横向溢出或控件重叠，控制台无错误。

### 25.3 自动验证

- Parser：106 项通过，1 项可选离线 E2E 跳过。
- 前端：33 项通过；桌面端：3 项通过。
- Vite：生产构建通过。
- 坐标门禁：`null` 不再被归一化为 `0`；超出 `0-100` 的地图百分比坐标会降级，不能进入 L3 精确定位。

### 25.4 未完成清单

- 15 个不同 Replay、五位置各 3 场的固定回归。
- 眼位、购买、死亡、目标和 TP 的玩家报告洞察生产器。
- 三个关键时刻从阶段故事升级为事件实体。
- 全发布视口矩阵和 EXE 安装、启动、解析、冷重启、卸载冒烟。

### 25.5 战斗时机行为诊断第一阶段

本轮在阶段 D 上继续完成“具体行为模式 -> 多个发生点 -> 对应战斗 -> 有界播放 -> 返回报告”的纵向闭环。

已实施：

- Parser 新增 `player-combat-timing/1.0`，空间到场与首次远程行动分开计算。
- 输出逐战斗事实、行为模式、普通模式资格、抑制原因、发生点列表和跳转目标。
- 角色置信度、空间可达性、存活状态、位置职责、样本数和后果证据均为负面结论硬门禁。
- 普通模式最多显示三个具体发生点；深度模式保留所有可审计事实和门禁原因。
- 同一主战斗中的泛化战斗职责问题会被更具体的战斗时机模式替换，跨战斗的独立问题仍可保留。
- 深度战斗模块新增五列时机比较，并支持逐行打开对应战斗。
- 新增真实 Replay 自动回归脚本和 1024 宽度侧栏 Grid 回归测试。

真实 Replay `8894766243`：

| 指标 | 结果 |
| --- | ---: |
| 解析耗时 | 40.581 s |
| 玩家报告 | 10 |
| 战斗时机事实 | 190 |
| 行为模式 | 22 |
| 普通模式模式 | 10 |
| 被门禁抑制事实 | 112 |
| 远程行动早于空间到场 | 36 |
| 无效发生点跳转 | 0 |

具体交互验收：

- 树精卫士普通问题包含 `06:48 上路 +24秒`、`11:33 中路 +13秒` 两个独立按钮。
- 两次跳转分别选中 `06:49-07:12` 和 `11:33-11:56`，列表中心偏差分别为 `0 px` 和 `2 px`。
- 两个有界复盘范围分别为 `06:38-07:18` 和 `11:23-12:01`。
- 首个范围在 4 倍速下于 `07:18.017` 自动暂停，误差 `17 ms`。
- 返回玩家报告后保留所选玩家、具体问题和两条发生点。
- 1440×900 与 1024×768 均无页面横向溢出；1024 下展开侧栏不再把主区压缩到 64px。
- 深度分析显示 19 条该玩家的战斗时机事实，五组字段完整，控制台错误为 `0`。

自动验证：

- Parser 120 项通过，1 项可选离线 E2E 跳过。
- 前端 36 项、桌面端 3 项通过。
- Vite 生产构建通过。

未完成：

- 尚未达到 15 个不同 Replay、五位置各 3 场的发布回归门槛。
- 尚未建立按 Patch、英雄和技能状态计算的关键技能可释放模型，本阶段不会输出“关键技能没交”的强批评。
- 尚未接入导航网格、TP 引导和位移技能的精确旅行时间，可达性仍为校准坐标与世界距离近似。
- 主包和图表运行时仍有大于 500 kB 的构建告警，需在玩家报告功能稳定后继续拆包。

## 26. 2026-07-26 阶段 A：player-report/4.0 计分协议

本轮优先完成评分协议、可重算审计和第一场真实 Replay 回归，没有继续扩张普通模式页面内容，也没有宣称 0.5.0 已达到发布门槛。

### 26.1 已实施

- 玩家报告主协议升级为 `player-report/4.0`。
- 每个可用维度统一输出 `base_score`、`behavior_modifier` 和 `final_score`，旧字段 `score` 与最终分保持一致。
- 综合分统一输出基础综合分、行为修正、最终综合分、有效权重和本地重算所需字段。
- 接入三种计分路径：
  - `embedded`：该结果已经包含在维度基础模型中，只保留审计记录，不再次修改分数。
  - `modifier`：满足独立行为、置信度、归责和证据门禁后，才进入行为修正。
  - `context_only`：仅用于解释背景，不参与分数。
- 同一结果使用稳定 `dedupe_key` 去重；出现竞争证据时，优先保留绝对影响更大、置信度更高且标识稳定的一条。
- 行为修正依次执行维度原始影响、置信系数、归责系数、同结果去重、单根因综合封顶、维度封顶和全场综合封顶。
- 单根因综合负向影响上限为 6 分；全场维度行为修正上限为 `[-15, +10]`，综合行为修正上限为 `[-12, +8]`。
- `score_card.audit` 使用 `player-report-score-audit/1.0`，记录三路计分数量、重复抑制数、服务端重算结果和封顶参数。
- 前端新增独立重算器，不直接相信服务端的“有效”标志；综合分、维度分和行为修正均按协议字段重新计算并比较。
- 前端重算只接受 `modifier` 的非零 `applied_delta`，并将服务端提供的允许误差硬封顶为 `0.05`；`embedded`、`context_only` 或未知路径出现非零值时直接判定协议异常。
- 综合分支持下钻到维度贡献；维度支持下钻到基础组件、评分路径、比较对象、证据事实和缺失门禁；行为根因支持下钻到置信度、归责系数、原始影响和实际应用值。
- 旧版 V3 报告显示明确的升级提示，不再暴露内部缺失字段代码。

### 26.2 TDD 覆盖

后端测试先以缺少 V4 计分器失败，随后覆盖：

- 基础分、行为修正和最终分公式。
- `embedded`、`modifier`、`context_only` 三路分流。
- 同一结果只能有一条非零行为修正。
- 重复证据选择绝对影响更大的候选。
- 单根因综合扣分不超过 6。
- 缺少目标维度和低置信度证据只能进入 `context_only`。
- 维度与综合分均可由输出协议重新计算。
- 分数缺失时不按 0 分处理。

前端测试覆盖：

- 综合分和维度分独立重算。
- 服务端错误声明不能覆盖前端重算结果。
- 服务端保存的维度最终分不能覆盖前端从基础分与组件重新得到的最终分。
- 服务端不能通过放大重算容差掩盖分数不一致。
- `embedded` 和 `context_only` 的非零应用值会使审计失败。
- 重复非零 `dedupe_key` 会使审计失败。
- V4 报告升级状态、旧版兼容提示和审计交互入口。

真实回归工具另外通过伪造根因摘要的失败测试，确认会使用 `scoring_impacts[].applied_delta` 与维度有效权重独立复算单根因综合负向影响，而不是信任服务端的 `scoring_summary`。

### 26.3 真实 Replay 协议回归

固定样本：`8894766243`

| 指标 | 结果 |
| --- | ---: |
| Replay 大小 | 83,534,160 bytes |
| 原始事件 | 267,683 |
| 本轮端到端解析耗时 | 61.509 s |
| 玩家报告协议 | `player-report/4.0` |
| 玩家报告数量 | 10 |
| 协议校验通过 | 10 / 10 |
| 行为修正范围 | -0.83 至 +0.97 |
| 重复非零扣分键 | 0 |
| 最大单根因综合负向影响 | 1.29 |
| 协议错误 | 0 |

交互验收：

- 电炎绝手综合分可由 `59.05 - 0.81` 重算为 `58.24`。
- “地图与节奏”可由 `48.00 - 1.20` 重算为 `46.80`。
- “连续冲突中的到场时机偏晚”可展开三个跨维度影响，显示置信度 80%、归责系数 50% 和各项实际应用值。
- 1280×720 下审计抽屉、9 个可用维度和根因明细均无横向溢出。
- 页面英雄图标全部加载成功，控制台未观察到与该流程相关的错误。

自动验证：

- Parser：133 项通过，1 项可选离线 E2E 跳过。
- 前端：71 项通过。
- 桌面端：3 项通过。
- Vite 生产构建通过；保留两个大于 500 kB 的既有分包告警。

### 26.4 阶段 A 边界

以下内容仍未完成，不能因为协议升级而视为已经具备：

- 当前 `base_components` 先以 `existing_dimension_model` 诚实承接 V3 维度结果，尚未把十个维度全部拆成真正的原子指标贡献。
- 当前独立行为修正白名单优先接入 `combat_timing`；其余根因继续按 `embedded` 或 `context_only` 处理，避免提前重复扣分。
- 尚未完成 15 个不同 Replay、五位置各 3 场的固定回归。
- 普通模式的内容收敛、三关键事件实体化和全部位置模板复核属于后续阶段，不在本轮阶段 A 范围内。
- 尚未完成 1024×720、1460×920、1920×1080 全视口矩阵以及 0.5.0 EXE 全流程验收。

### 26.5 下一步

阶段 A 后续优先级：

1. 将十个维度的 `existing_dimension_model` 逐步拆成可解释的原子基础组件，并为每个组件建立 Golden 测试。
2. 按“独立行为且未进入基础分”的原则扩展 modifier 白名单，优先评估资源冲突、目标前准备和明确的可归责视野行为。
3. 建立 15 场 Replay 固定样本矩阵，先验证协议稳定性、重复扣分和位置串位，再继续普通模式产品化。
4. 完成阶段 B 的普通模式内容选择，严格收敛为一句总评、三个关键时刻、一个优点、一个主要问题和一个训练目标。

## 27. 2026-07-28 统一事件候选池与 0.5.0 发布门槛验收

本轮完成阶段 B-E 的开发与验收，但没有发布 Git，也没有把安装包上传到更新源。

### 27.1 统一事件候选池

- 新增 `player-report-event-candidates/1.1`。
- 候选来源覆盖原报告洞察、眼位、关键装备、阵亡、地图目标、TP、兵线压力、装备送达、神符控制、辅助路线、疑似拉野和等级强势期。
- 每个候选保留类型、玩家、时间、实体、地图位置、复盘范围、事实等级、重要性、归责、普通模式资格和抑制原因。
- 不再用固定“对线、中期、后期”阶段故事充当关键时刻。
- 普通模式保留所有通过硬门禁的真正重要时刻，不再设置三个事件的硬上限。
- 普通事件时间轴提供“全部、做得好、需改进、重要事实”筛选；复杂指标和技术字段仍只在专业模式展开。

### 27.2 去重与准入

- 同一场战斗产生的阵亡、TP 和战斗洞察合并为一个主事件。
- 同一玩家 5 分钟内的高价值眼位聚类，避免连续插眼刷屏。
- 同一 120 秒窗口内的高地推进目标聚类。
- 肉山、不朽之守护和肉山尝试按时间桶聚类。
- 普通塔、信使、低价值眼位、无归责阵亡和无上下文 TP 保留在候选池，但不会被提升为重要时刻。
- 坐标、时间或范围不完整的候选不能进入普通模式优缺点。
- 聚合强项只作为专业背景，不再伪装为可定位的个人优点。

### 27.3 精确跳转

所有普通模式优点和问题均具备：

- 主时间与开始/结束范围。
- 地图区域和校准坐标。
- 实体类型与实体 ID。
- 对应模块。
- 有界播放范围。
- 返回玩家报告所需上下文。

真实 Replay `8894766243` 验证：

- 点击 `11:23-12:01` 的“连续冲突中的到场时机偏晚”后，进入中路团战并把比赛游标定位到 `11:33.334`。
- 复盘条显示原问题、模块和范围，并支持“从头播放”和“返回玩家报告”。
- 返回后恢复玩家、简明模式、报告内容和原定位上下文。

### 27.4 双层固定 Replay 回归

正式结果：

| 指标 | 结果 |
| --- | ---: |
| 正式 Replay | 15 / 15 |
| 全员协议报告 | 150 / 150 |
| Golden 报告 | 15 / 15 |
| 1/2/3/4/5 号位全员报告 | 各 30 |
| 聚合兜底报告 | 0 |
| 重复应用键 | 0 |
| 最大单根因综合负向影响 | 1.91 |
| 单根因负向上限 | 6.00 |

内容 QA：

| 指标 | 最小 | 平均 | 最大 |
| --- | ---: | ---: | ---: |
| 候选事件 | 50 | 90.40 | 146 |
| 重要时刻 | 10 | 18.60 | 27 |
| 明确优点 | 0 | 5.33 | 15 |
| 主要问题 | 0 | 3.15 | 9 |

- 优缺点缺少行动建议：0。
- 优缺点缺少可定位有界跳转：0。
- 单份报告重要时刻超过 30：0。
- 15 名 Golden 重点玩家的位置与预期不一致：0。

### 27.5 桌面与自动验收

- Java：183 项执行，0 失败，1 项可选测试跳过。
- 前端、工具和桌面 Node 测试：238/238 通过。
- 四档玩家报告窗口通过：1024x720、1280x800、1460x920、1920x1080 请求。
- 1920x1080 受当前物理显示器限制，实际内容视口为 1693x982；其余三档达到目标内容视口。
- 五个附加页面通过：发育、打钱、战斗、视野和设置。
- 所有截图破损英雄图片为 0，断言范围内横向溢出为 0。
- 0.5.0 EXE 完成静默安装、内置 Java 21、Parser 1.7.0、Replay 导入、取消清理、285,013 条事件完整解析、玩家识别、手动切换持久化、冷重启和卸载。

安装包：

- `release/Dota-Lens-Setup-0.5.0-x64.exe`
- 大小：158,922,759 bytes。
- SHA-256：`C244F359F827090FCFEB5586DAD0AD34ADE0C43FA5F18F5C8025B081826F2195`
- 当前未签名、未发布。

### 27.6 防卡与已知边界

- 回归脚本不再让 `KeepParser` 子进程占住命令生命周期，运行进度写入可轮询 JSON。
- EXE 布局用例逐项输出进度，每个窗口有 90 秒硬超时，不再无限 `-Wait`。
- EXE 冒烟每通过一项就输出检查结果，长解析每 5 秒输出任务阶段。
- 受限开发沙箱会阻止 Electron 渲染和 GPU 子进程启动；GUI 验收必须在允许本地子进程的环境执行。这不是产品 EXE 的启动失败。
- Vite 仍报告主入口和图表运行时两个分包大于 500 kB；下一阶段优先处理 JSON 体积、farm 轻量接口和首屏性能预算。

## 28. 2026-07-29 P1 事实接入与最终发布候选验收

### 28.1 P1 已接入解析器

以下内容由 Parser API `1.7.0` 直接生成，不是前端模拟：

- `lane_pressure`：兵线交汇、推进深度和安全门禁事实。
- `item_delivery`：购买到首次在可用栏位观察到的送达延误。
- `rune_control`：神符拾取、位置与可定位收益事实。
- `support_route`：辅助离线窗口、路线与核心承压上下文。
- `suspected_pull`：满足时间、位置和单位事实门禁的疑似拉野。
- `level_spike`：到达关键等级后的 45 秒利用窗口。

这些候选默认走 `context_only` 或既有基础分路径，不因新增展示而重复扣分。缺少连续敌方可见性、逐单位生命周期、合理目标或位置职责时，只保留复核线索。

### 28.2 固定样本结果

15 名 Golden 玩家共生成 1,356 个候选：

| P1 类型 | 数量 |
| --- | ---: |
| 兵线压力 | 220 |
| 装备送达 | 40 |
| 神符控制 | 45 |
| 辅助路线 | 41 |
| 疑似拉野 | 9 |
| 等级强势期 | 56 |

所有 15 份 Golden 的综合分和各维度分与升级前保持一致，证明新增 P1 事实没有暗中改变计分。

### 28.3 最终桌面验收

- 布局脚本会先绑定分析玩家，并硬检查目标页面可见、玩家选择弹窗关闭、英雄图完整和横向溢出。
- 四档玩家评分和发育、打钱、团战、视野、设置共 9 个用例通过。
- 1024 宽度下视野筛选器完整显示“全部玩家、全部眼型、全部用途”。
- 最终 EXE 26/26 冒烟检查通过，真实 Replay 完整解析耗时 29.790 秒。
- 生产依赖 `npm audit --omit=dev` 结果为 0 vulnerabilities。
