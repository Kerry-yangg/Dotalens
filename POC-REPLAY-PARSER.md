# OpenDota 普通解析入口 POC 结论

日期：2026-07-16  
状态：M1 技术路线验证通过，尚未生产化

## 1. 结论

这个方向可行。

我们已经使用 OpenDota 官方 parser 源码构建本地服务，并把两份真实 `.dem`
回放直接 POST 到 `http://127.0.0.1:5600/`。请求没有使用 `/blob` 或
`?blob`，返回的是逐行 JSON，而不是裁剪后的聚合对象。

两场回放均获得了十名玩家从开局到结束、每个整数秒一条的 `interval`
快照。正式比赛阶段没有缺秒、重复秒或无效 JSON 行，位置、金钱、净值、
经验和补刀字段覆盖率均为 100%。

## 2. 验证环境

- Windows + PowerShell 7.6.3
- Eclipse Temurin Java 21.0.11
- Apache Maven 3.9.12
- OpenDota parser commit `a03b9e5f228cecb3f2e3ddfd378683bbb1d467fa`
- parser JVM 参数：`-Xms256m -Xmx2g`
- parser 固定监听：`127.0.0.1:5600`

## 3. 实测结果

| 比赛 ID | replay 大小 | 热 JVM 请求耗时 | 原始 JSONL | gzip | JSON 对象 | interval | 正式比赛时间 | 十人总缺秒 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 7121652244 | 69.89 MiB | 2.31 秒 | 26.93 MiB | 1.13 MiB | 130,492 | 21,960 | 0-1965 秒 | 0 |
| 7147939538 | 90.75 MiB | 2.65 秒 | 31.89 MiB | 1.32 MiB | 147,158 | 25,240 | 0-2280 秒 | 0 |

第一场冷 JVM 首次请求为 4.30 秒。以上耗时是本机 POC 数据，不应直接当作
云服务器 SLA。gzip 使用 .NET `SmallestSize`，两份 JSONL 都压缩到原体积约
4%，说明“本地解析、压缩分析包、同步到小程序”在传输体积上可行。

95 MB 回放解析期间观测到 Java 工作集最高约 566 MB、私有内存约 664 MB；
空闲启动时工作集约 53 MB。JVM 会保留已申请堆，因此生产版应使用单任务队列
和进程级资源限制，不能按 HTTP 请求无限并发。

## 4. 原始数据确认

正式比赛阶段每名玩家每秒都有以下快照字段：

- `time`、`slot`、`hero_id`、`unit`
- `x`、`y`、`life_state`、`level`
- `gold`、`networth`、`xp`、`lh`、`denies`
- 击杀、死亡、助攻、插眼、拉野、叠野、神符等累计状态

第一场还保留了以下原始事件：

| 事件 | 数量 |
| --- | ---: |
| 玩家操作 `actions` | 65,475 |
| 伤害 | 13,787 |
| 单位死亡 | 2,256 |
| 金钱 | 2,133 |
| 经验 | 2,768 |
| 物品使用 | 840 |
| 购买 | 475 |
| 假眼放置 | 30 |
| 真眼放置 | 47 |

例如在 120 秒，原始事件明确记录了小精灵击杀
`npc_dota_neutral_gnoll_assassin`，同秒还有中立收益金钱和经验事件；再关联
该玩家 120 秒的坐标快照，就能得到“何时、在哪里、击杀什么野怪、获得多少
收益”的证据链。

## 5. 可以生成的分析

以下内容可以直接或高置信度生成：

1. 每秒位置、经济、经验、补刀、等级和存活状态曲线。
2. 中立单位逐次死亡记录，以及击杀英雄、时间和单位类型。
3. 使用地图版本对应的野区营地多边形，把死亡事件归属到具体野点。
4. 将连续中立击杀聚合为刷野片段，并用金钱事件和净值差交叉校验收益。
5. 把刷野、兵线、回城、死亡、团战、插眼和出装放到同一时间游标中联动。

“因为刷野漏了多少兵”不是 replay 中现成的官方字段，只能做推导：统计对应
时间窗的兵线单位死亡，识别玩家是否完成最后一击，再结合玩家坐标、分路、
兵线归属和到达时间计算机会损失。产品必须显示置信度，不能把反事实估算标成
确定事实。

## 6. 当前 parser 的缺口

1. `Entry.hero_inventory` 在上游代码中被标记为 `transient`，普通 JSONL 不会
   输出每秒背包。第一版可从购买、物品和初始装备事件重建；要做严格的每秒
   背包状态，应在自有 parser 分支中显式序列化库存变化。
2. 上游 `Parse` 仍把所有 `Entry` 放进 `finalList`，回放结束后才写 JSONL；源码
   中已有 TODO 提示普通模式可以直接流式输出。生产版应改成边解析边写，降低
   大回放的堆占用和首字节等待。
3. HTTP handler 在开始解析前就发送 200，并吞掉解析异常。适配层必须验证
   `epilogue`、JSONL 完整性和关键字段，再决定任务成功或失败。
4. 上游服务含固定端口、OpenDota 注册定时器，且没有鉴权、限流和文件大小
   限制。个人版只应绑定本机，云端版必须放在受控任务队列后面。
5. replay 获取和解析是两个独立问题。过期 replay 无法靠 parser 恢复，应在比赛
   结束后尽快下载并保存。

## 7. 推荐下一步

1. 建立最小 parser 分支：移除注册逻辑、增加健康/任务状态、严格错误码、原始
   JSONL 流式输出和库存变化事件。
2. 增加 JSONL 标准化器，按 `match_id/player_slot/time_chunk` 生成 gzip 分片和
   小程序所需的摘要索引。
3. 先实现刷野证据链：中立死亡事件、击杀者、逐秒坐标、营地多边形、金钱与
   经验校验、连续片段聚合。
4. 再实现兵线机会模型，并把“事实事件”和“估算损失”分层展示。

可重复执行脚本位于 `tools/replay-poc/`。原始 replay 和几十 MB 的 JSONL 没有
放进仓库，避免污染项目；脚本会在指定输出目录生成 JSONL 和验证摘要。
