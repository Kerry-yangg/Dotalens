# Dota Lens 战斗与打钱优化实施方案

状态：评审方案，尚未实施  
依据：`DOTA-LENS-COMBAT-FARM-JUDGMENT-LOGIC.md` 与当前解析器实现  
日期：2026-07-19

## 1. 优化结论

当前系统已经能够把 Replay 转成可回看的战斗、经济、位置、眼位和目标事实，但“事实存在”不等于“建议可信”。下一轮应按以下顺序推进：

1. 修正现有会误导用户的分类和字段语义。
2. 解决战斗片段串联、参与者漏算和视野不完整。
3. 建立真实兵线、营地、英雄状态与战争迷雾状态。
4. 在证据完整后再启用安全兵线、漏线和最优路线建议。
5. 最后引入机器学习做排序和校准，不让模型代替事实门禁。

核心原则：

- 没有真实兵线，不说“这波线可以收”。
- 没有真实营地状态，不说“这里可以线野双收”。
- 没有技能、魔法和控制状态，不说“这个人有技能没放”。
- 没有完整敌方可见性，不说“这条线一定安全”。
- 战斗类型、战斗重要性和个人职责必须是三个独立结果。

---

## 2. 优先级总览

| 优先级 | 优化项 | 解决的问题 | 预期结果 |
|---|---|---|---|
| P0 | 金钱来源字典 | 大量收益被错误合并为战斗经济 | 热力图和收入构成可信 |
| P0 | 打钱结论语义修正 | 最低窗口被当作异常、`actionable` 含义相反 | 页面不再误导用户 |
| P0 | 战斗片段二次切分 | 79 至 123 秒追击被串成一场 | 一次接触对应一个可复盘片段 |
| P0 | 战斗原子去重与归因 | DoT、重复日志、召唤物归因可能污染强度 | 强度和参与者更稳定 |
| P0 | 产品层保留已有状态字段 | 原始 Replay 有冷却、魔法和可见性，但分析层丢弃 | 为职责与视野门禁提供事实 |
| P1 | 连续英雄可见性 | 当前只看最近 12 秒伤害可见性 | 能还原“当时已知敌人在哪里” |
| P1 | 逐波兵线状态 | 无法确认安全线、漏线和到达截止时间 | 生成真实兵线机会对象 |
| P1 | 营地状态 | 无法确认营地是否存活、被占用或叠野 | 生成真实野区机会对象 |
| P1 | 动态 1 至 5 号位 | 核心/辅助二分过粗 | 职责与资源优先级更合理 |
| P1 | 团战责任门禁 | 当前固定公式容易误判“内鬼” | 只在可行动时评价责任 |
| P2 | 安全兵线与路线引擎 | 当前只是观察事实，不是真实反事实建议 | 输出收益、风险、路程与截止时间 |
| P2 | 自动训练与校准 | 固定阈值不能适配所有版本和阵容 | 模型辅助排序，硬门槛继续兜底 |

---

## 3. 阶段一：先修正现有错误结论

这一阶段不依赖新 Replay 字段，应先完成。

### 3.1 完整金钱来源字典

当前问题：只有 `gold_reason=13` 被归为兵线、`14` 被归为中立，其余 `11..21` 全部归入 `combat`。

实施方案：

1. 新建 `GoldReasonCatalog`，按比赛 patch 加载原因字典。
2. 至少区分：
   - 兵线小兵；
   - 普通中立；
   - 远古；
   - 英雄击杀；
   - 英雄助攻；
   - 防御塔/兵营/基地；
   - 肉山；
   - 信使；
   - 眼位与召唤物；
   - 赏金符及其它地图资源；
   - 被动金钱；
   - 其它/未收录。
3. 金钱事件与同秒附近的单位死亡、目标事件、神符事件交叉校验。
4. 无法分类时保留原始 `gold_reason`，显示“未收录来源”，不能并入战斗。
5. 输出每种原因的事件数、金钱总量和未映射率。

验收：

- 每笔正负金钱事件都保留原始 reason 和最终来源。
- 未映射原因不再进入任何已知分类。
- 同一比赛所有来源金额之和与原始金钱事件总和一致。
- 热力图、30 秒窗口和单位击杀表使用同一来源字典。

### 3.2 修正低收入窗口语义

当前问题：没有窗口低于阈值时，程序仍取最低 5 个窗口，容易让用户误以为这些窗口一定有错误。

实施方案：

1. 将结果拆成两组：
   - `anomalies`：真正低于异常阈值的窗口；
   - `relative_lows`：本场相对最低窗口，只用于复盘，不标记错误。
2. 没有异常时明确显示“本场未发现满足门槛的低收入异常”。
3. 只有 `anomalies` 可以进入建议系统。
4. 异常阈值除个人中位数外，再加入同英雄、同位置、相近比赛时长基线；基线不足时只用个人本场分布。

### 3.3 修正打钱结果字段

当前问题：`actionable=true` 实际表示“行为与最高分候选一致”，不是“需要采取行动”。

建议协议：

| 新字段 | 含义 |
|---|---|
| `observed_action` | 玩家实际做了什么 |
| `best_supported_option` | 当前证据下最高分候选 |
| `matched_best_option` | 实际行为是否匹配候选 |
| `review_required` | 是否存在值得复核的不一致 |
| `recommendation_enabled` | 是否满足建议证据门槛 |
| `evidence_status` | `complete/partial/missing/invalid` |

迁移时保留旧字段一个版本，但前端不再消费 `actionable`。

### 3.4 拆开 `combat_or_idle`

当前问题：参战、赶路、补给、等待、死亡和真正空转都被合并。

第一阶段先基于已有事实拆成：

| 类型 | 判断证据 |
|---|---|
| `combat` | 窗口内有有效战斗参与行为 |
| `dead` | 玩家死亡或处于复活等待 |
| `teleport_or_travel` | 有 TP 或连续位置移动，且没有资源收益 |
| `base_or_recovery` | 位于基地/泉水附近并恢复生命或魔法、购买物品 |
| `waiting_resource` | 位于已确认资源区域，但资源状态尚不完整 |
| `idle_suspected` | 存活、低移动、无收益、无战斗、无购买/恢复证据 |
| `unknown` | 数据不足，无法解释 |

只有 `idle_suspected` 且证据完整时才能进入“可能空转”提示；`unknown` 不做负面评价。

---

## 4. 阶段二：战斗识别 2.1

### 4.1 战斗原子标准化

建议把 `ProductAnalysis` 中的战斗逻辑拆成独立流水线：

```text
CombatAtomNormalizer
        ↓
CombatCandidateBuilder
        ↓
CombatSegmenter
        ↓
CombatParticipantResolver
        ↓
CombatClassifier
        ↓
CombatContextAnalyzer
```

原子标准化需要完成：

1. 使用 `game_time_ms + demo_tick + event_seq` 去重同一日志。
2. 伤害区分：普攻、主动技能、DoT、反射、自伤、召唤物、幻象、建筑和环境。
3. 召唤物、幻象和反射伤害归属到正确玩家，同时保留来源单位。
4. 小兵、塔、泉水和肉山对英雄的伤害只做上下文，不进入英雄输出强度。
5. 同一施法产生的多条日志合并为一个施法实例，同时保留每段伤害。
6. 纯 DoT tick 可以计入总伤害，但不能无限刷新“仍在交战”的结束时间。

### 4.2 长战斗二次切分

当前 6 秒聚类和 5 秒合并应保留为第一阶段候选生成。之后增加 `CombatSegmenter` 二次切分。

建议初始切分规则：

#### 规则 A：有效接触空窗

- 连续 `7 秒` 没有新的主动伤害、控制、死亡或有效战斗施法，切分。
- 仅剩 DoT、光环、被动触发或附近移动时，不重置空窗。

#### 规则 B：中心跃迁与目标切换

满足以下全部条件时切分：

1. 前后 4 秒战斗中心距离至少 `12%` 地图跨度。
2. 主要受击目标集合 Jaccard 相似度低于 `0.35`。
3. 共享有效参与者少于 50%，或共享者没有连续行动。

#### 规则 C：双地点同时冲突

在同一 3 秒窗口内出现两个空间峰，并满足：

- 两峰中心距离至少 `14%`；
- 每峰双方至少各 1 人；
- 每峰独立达到最低战斗门槛；

则拆成两场，不取中点。

#### 规则 D：保留真实追击

以下条件满足时不因中心移动而切分：

- 主要攻击链连续；
- 参与者重合至少 50%；
- 每 6 秒内都有新的主动伤害、控制或死亡；
- 战斗中心沿连续路径移动，不是突然跨区域跳变。

这些阈值是第一版工程起点，必须用多场分布和合成边界测试调整，不能直接宣称为真实语义最优值。

### 4.3 有效参与者升级

为每位附近英雄输出参与状态：

| 状态 | 定义 |
|---|---|
| `active_damage` | 造成或承受有效英雄伤害 |
| `active_control` | 施加或承受有效控制 |
| `active_protection` | 产生有效治疗、护盾、驱散或救援 |
| `active_cast` | 使用了与战斗相关的技能或物品 |
| `victim_only` | 只被击杀或承受伤害，未观察到主动动作 |
| `nearby` | 在附近但没有有效行为 |
| `arriving` | 正在进入战场，正式接触后才行动 |
| `leaving` | 前期参与，之后撤离或追击脱离 |

团战人数仍只统计有效参与者，但页面能解释“为什么算他”和“为什么没算附近这个人”。

新增每人字段：首次行动、最后行动、有效行动秒数、战场存在率、到达延迟、离场时间、证据事件 ID。

### 4.4 五分类保留硬门槛，先不盲调数字

以下产品约束继续保持：

- 2v2 永远是小规模冲突。
- 团战至少 5 名有效参与者，双方至少 2 人。
- 关键 2v2 通过重要性进入关键复盘，不通过改类型实现。
- 无死亡高投入接触可以是小规模冲突。
- 非兵线区域不能标记线上换血。

优化重点应是特征质量和切分质量。完成后再重新统计：

- 各类型数量分布；
- 片段时长分布；
- 人数分布；
- 强度和生命池伤害比分布；
- 规则边界附近样本。

只有发现稳定偏差后，才调整 `60` 强度、`5 秒` 持续时间、`0.40` 人数平衡等阈值。

### 4.5 增加正交战斗标签

主类型仍保持五类，新增更有复盘价值的上下文标签：

- 越塔强杀、塔下反打、守塔；
- 烟雾抓人、反蹲、TP 支援、多人连续 TP；
- 肉山试探、肉山争夺、抢肉、抢盾；
- 高地进攻、高地防守、基地保卫；
- 买活返场、盾触发、关键技能交换；
- 无死亡逼退、目标区控场；
- 长追击、撤退失败、双地点冲突；
- 以少打多、以多打少、先手成功、反手成功。

这些标签用于回答“这是什么场景”，不改变“它属于哪一种战斗”。

---

## 5. 阶段三：利用 Replay 已有状态补齐团战视野和职责门禁

代码审计发现，解析器原始 `interval` 已经输出：

- 英雄生命、魔法、移动速度和存活状态；
- `visible_by_team`、日夜视野、战争迷雾队伍；
- 完整物品栏及物品冷却；
- 已学习技能、技能等级、冷却和魔法消耗。

但 `ProductAnalysis` 当前没有保留移动速度和可见性，`compactInventory` 丢弃了冷却，技能状态也没有进入产品分析索引。因此这部分主要是产品索引改造，不需要重新发明数据来源。

### 5.1 状态索引改造

为每名玩家建立按秒或状态变化压缩的：

```text
HeroStateTimeline
- hp / max_hp
- mana / max_mana
- position / move_speed / life_state
- visible_by_team
- inventory: item, slot, charges, cooldown
- abilities: id, level, cooldown, mana_cost
- control modifiers: stun, silence, root, disarm, mute
```

只在状态变化时写入产品包，避免每秒重复完整物品和技能数组。

### 5.2 连续可见性

1. 使用英雄 `visible_by_team` 和 `visibility` 事件生成每方可见区间。
2. CombatLog 的 `visible_radiant/visible_dire` 作为交叉校验锚点。
3. 眼位几何只解释视野来源，不替代真实可见状态。
4. 对不可见敌人保留最后出现位置、最后出现时间和移动可达范围。
5. 烟雾、隐身、真视和高台缺少可靠实体状态时，明确降级置信度。

团战页应回答：

- 开战前 10 秒双方分别看见了谁；
- 第一次发现敌人的时间差；
- 先手目标当时是否已可见；
- 战场中心是否有假眼/真眼覆盖；
- 视野来自眼、英雄、塔、兵线还是仅能确认 CombatLog 可见。

### 5.3 动态位置与职责

角色不再只按 10 分钟净值前三名分核心。

建议融合：

- 0:45 至 7:00 分路位置；
- 补刀、净值、经验和购买优先级；
- 安全路/中路/劣势路双人组关系；
- 10 分钟后资源占比和地图路线；
- 是否发生换线或位置转型。

输出 `position=1..5`、有效时间段和置信度。置信度低于 `0.65` 时只显示事实，不计算职责分。

### 5.4 团战责任硬门禁

在产生任何负面问题标签前，先检查：

1. 玩家当时存活且已到达可影响战斗的位置。
2. 技能已经学习。
3. 技能或物品冷却完成。
4. 当前魔法足够。
5. 玩家未被沉默、眩晕、缴械、禁用物品或其它对应限制影响。
6. 存在合法目标且在施法范围内。
7. 技能在该阶段仍有价值，而不是战斗已经不可挽救。
8. 玩家职责与英雄能力确实要求该行为。

输出层级改为：

```text
事实：技能未使用
可用性：可用 / 不可用 / 未知
职责相关性：高 / 中 / 低 / 未知
结论：执行问题 / 可复核 / 不评分
```

辅助“没插真眼”只在有隐身威胁、高台、肉山、关键目标区或明确排眼需求时检查。普通线上消耗不再自动扣分。

---

## 6. 阶段四：建立真实兵线和营地状态

这是安全兵线、漏线和最优路线的必要前置。

### 6.1 当前可复用的数据基础

解析器已经默认输出 `unit_enter/unit_state/unit_left/visibility`，并能把实体分为英雄、兵线小兵、中立、建筑、信使和受控单位。

当前不足：单位事件主要在进入、生命状态和可见性变化时输出，没有连续记录兵线单位移动，也没有把这些事件接入 `ProductAnalysis`。

### 6.2 兵线状态设计

不建议把每个小兵的每一帧位置写入产品包。建议生成两个层级：

#### 逐单位结算事实 `CreepResolution`

- 单位实体 ID；
- 队伍、小兵类型、所属路线；
- 波次 ID 和出生时间；
- 死亡时间与位置；
- 最后击杀者、反补者或无人获得；
- 实际金钱与经验归属；
- 是否被召唤物、塔或中立单位击杀。

#### 每 2 秒波次状态 `LaneWaveState`

- 波次 ID、路线和队伍；
- 存活近战/远程/旗手/攻城车数量；
- 波次中心、前锋位置和移动方向；
- 当前可获得金钱与经验范围；
- 最近建筑和预计到达时间；
- 正在覆盖该波的玩家槽位。

波次 ID 由 patch 刷新规则、出生时间、队伍和路线共同确定，实体事实用于校正时钟。

### 6.3 营地状态设计

由 `MapCoordinateService` 按 patch 提供营地多边形和类型：小、中、大、远古。

生成 `CampState`：

- 营地 ID、类型和位置；
- 当前单位数与单位组合；
- 最近刷新时间；
- 是否被单位/眼/英雄阻挡刷新；
- 是否叠野及叠层数；
- 开始清理、清理完成和清理者；
- 是否已被队友声明或正在清理；
- 下一次刷新截止时间。

叠野贡献分开记录：

- `stack_creator`：完成叠野的人；
- `stack_clearer`：获得直接收益的人；
- `team_value_created`：叠野增加的团队资源；
- `personal_gold`：个人实际获得收益。

### 6.4 动态比赛阶段

保留 10/20 分钟作为检查点，不再把 7:30 和 15:00 作为固定阶段边界。

每 30 秒为双方分别判断：

| 阶段 | 主要证据 |
|---|---|
| `laning` | 三路线稳定占用、线上收入占主导、外塔存活、低聚集度 |
| `lane_transition` | 换线增加、辅助游走、第一座塔或关键物品带来结构变化 |
| `farm_distribution` | 线野收入并行、核心资源区分配、战斗频率较低 |
| `map_control` | 视野前压、多人入侵、肉山/魔方准备、外塔推进 |
| `high_ground` | 高地建筑附近聚集、兵线推进和高地技能交换 |

前 10 分钟仍是重点分析区间，但阶段由行为证据决定。

---

## 7. 阶段五：安全兵线、漏线和路线建议

### 7.1 先生成“机会对象”，再生成建议

每个可用资源先成为独立机会：

```text
ResourceOpportunity
- id
- kind: lane_wave / camp / stack / rune / lotus / objective
- available_from / expires_at
- expected_gold_range / expected_xp_range
- location / route_eta
- owner_priority / ally_conflict
- observed_enemy_threats / missing_enemy_uncertainty
- vision_support / escape_support / tp_support
- evidence_status / confidence
```

任何建议都必须引用具体机会 ID。机会不存在或状态不完整时，不允许只根据刷新时钟画路线。

### 7.2 安全兵线硬门槛

一波线只有同时满足以下条件，才能进入“建议收线”候选：

1. 该波真实存在且仍有可获得资源。
2. 玩家预计到达时间早于资源失效时间，并保留至少 3 秒容错。
3. 没有更高资源优先级队友已经在收或即将到达。
4. 玩家存活，路线可达，且不是必须参加正在发生的关键目标事件。
5. 敌方威胁等级不超过该位置/英雄允许门槛。
6. 数据覆盖满足兵线、玩家位置和敌方可见性门禁。

第一版只输出低/中/高风险，不输出伪精确的死亡概率。

### 7.3 敌方威胁模型

不能继续把每个未知敌人固定加 12 分，也不能默认野区比兵线安全。

每名敌人单独建立威胁：

- 最后出现位置和距现在的时间；
- 移动速度、TP、传送门和全图移动能力；
- 跳刀、隐身、先手距离；
- 控制链和爆发能力；
- 当前可见性、烟雾/隐身不确定性；
- 该敌人在最近 30 至 60 秒的路线方向；
- 己方眼、塔、兵线、英雄和逃生路径提供的保护。

输出三个组成项：

```text
到达威胁：敌方是否可能在资源失效前抵达
击杀威胁：到达后是否具备控制和爆发
信息不确定性：当前判断缺少多少视野证据
```

不确定性高时降低建议置信度，而不是直接断言危险或安全。

### 7.4 漏线计算

对每个兵线单位死亡进行归因：

1. 玩家是否按当前实际路线本可在死亡前到达经验/补刀区域。
2. 该单位收益是否被队友获得。
3. 玩家当时是否死亡、补给、参战、守塔、控符或准备关键目标。
4. 兵线是否因敌方威胁而不适合进入。
5. 玩家当时是否选择了另一项可确认的资源。

输出必须区分：

| 类型 | 含义 |
|---|---|
| 个人让线 | 队友获得，个人少拿但团队未损失 |
| 团队漏线 | 小兵死亡且己方无人获得 |
| 合理放弃 | 因死亡、危险或关键目标无法收取 |
| 刷野取舍 | 玩家选择可确认营地，放弃可到达兵线 |
| 移动/补给代价 | 因路线或恢复行为错过 |
| 可疑空转 | 无其它合理行为且本可到达 |
| 证据不足 | 无法确认兵线、威胁或路线 |

只有“刷野取舍”和“可疑空转”可以进入训练建议。队友收掉的线不能算团队损失。

### 7.5 路线类型

建议系统输出明确路线类型，不只给一个地图点：

- 只收当前兵线；
- 收线后单营地；
- 收线后双营地再回线；
- 先叠野再回线；
- 野区清理后接下一波线；
- 神符/莲花顺路路线；
- 关键目标前压线后集结；
- 当前区域资源冲突，转移到另一资源区；
- 风险过高，放弃资源并撤退；
- 证据不足，不生成路线。

“双营地”必须实际存在两个可清营地，且英雄清理时间、移动时间和下一波兵线截止时间均满足，不能只根据分钟数推荐。

### 7.6 路线效用函数

建议先使用可解释的范围评分：

```text
路线价值 =
    可确保金钱
  + 经验折算价值
  + 叠野/推线/控符等战略价值
  - 路程机会成本
  - 死亡风险成本
  - 错过关键目标成本
  - 与队友资源冲突成本
```

输出收益范围和各分项，不只输出单一总分。初期权重按位置设置：

- 1/2 号位偏重个人金钱与经验；
- 3 号位加入危险线、推塔和团队空间价值；
- 4/5 号位偏重团队资源创造、叠野、视野、符和支援，不因未抢核心兵线而扣分。

---

## 8. 机器学习使用边界

机器学习可以优化排序和阈值，但不能补出 Replay 中不存在的兵线、营地和战争迷雾事实。

### 8.1 战斗分类

在至少 100 场 Replay、约 5,000 个候选后建立特征库：

- 人数、持续时间、伤害、生命池伤害比；
- 双方互换率、目标集中度、控制、施法密度；
- 交互图、空间散布、中心移动、事件空窗；
- 死亡、买活、TP、塔、肉山、视野和时间阶段。

训练方式：

1. 多组程序化标签函数产生弱标签。
2. 按比赛划分训练集和验证集，禁止同一比赛片段泄漏。
3. 轻量模型只在高置信区域覆盖软分类。
4. 2v2、最低人数、数据完整性、TP 完成和类型合法性继续由硬门槛控制。
5. 相似战斗检索只展示参考案例，不能把聚类簇自动命名为团战。

### 8.2 打钱路线

可以从真实 Replay 自动得到部分结果标签：

- 路线完成后 30/60 秒收入；
- 是否死亡；
- 是否错过兵线；
- 是否参与关键战斗；
- 是否与队友发生资源冲突。

但这些是玩家实际选择的结果，不等于未选择路线的反事实结果，存在明显选择偏差。因此模型第一阶段只能用于候选排序和风险校准，不能直接声称“另一条路线一定多 300 金”。

---

## 9. 前端呈现优化

### 9.1 战斗页面

- 战斗列表增加类型、重要性、人数、持续时间、死亡数和分类理由筛选。
- 对超过 45 秒的片段显示“长战斗复核”标记。
- 地图显示准备、先手、交战、收尾各阶段中心和参与者轨迹。
- 点击参与者可查看“为何算参战”和首末行动证据。
- 职责页先展示事实，再展示可用性门禁和结论。
- `不评分` 与 `执行问题` 使用完全不同的视觉状态。

### 9.2 打钱页面

- 热力图保持大地图，并支持 0-5、5-10、10-15、15-20 分钟与自定义区间。
- 低收入异常与相对低点分开展示。
- 时间轴同时显示兵线波次、营地、叠野、神符、莲花、死亡和战斗。
- 建议路线显示预期收益范围、风险组成、路程、截止时间和资源归属。
- 点击“漏线”必须跳到对应波次和小兵结算证据。
- 证据不足时显示缺失项，不画虚构路线。

---

## 10. 自动化验收

### 10.1 战斗

- 2v2 升级团战必须为 0。
- 无死亡、无双方有效回击的兵线接触不能成为团战。
- 同时发生在远距离区域的冲突必须拆开。
- 仅 DoT 或移动不能把战斗无限延长。
- 超过 45 秒的战斗必须存在每 7 秒内的主动接触连续证据，否则测试失败。
- 每名有效参与者必须至少有一条可点击证据。
- 主战场位置必须引用有效位置事件，不能回退地图中心。
- 缺少技能可用性时，负面责任判断数量必须为 0。

### 10.2 打钱

- 所有金钱事件金额守恒，未映射原因保留原值。
- 没有真实营地收益时，线野双收候选数量必须为 0。
- 没有兵线状态时，安全兵线和漏线建议数量必须为 0。
- 每条路线必须引用真实机会 ID、可达时间和截止时间。
- 队友获得的兵线不能计为团队漏线。
- 死亡、关键团战和目标准备窗口不能直接标记为梦游。
- 没有异常窗口时，页面不能显示“发现低收入错误”。
- 所有负面建议必须能跳转到原始事件证据。

### 10.3 性能

- 不保存每个单位的逐帧完整状态。
- 波次使用 2 秒聚合状态，单位使用出生/死亡/收益结算事实。
- 营地按状态变化输出，不做无变化的重复快照。
- 相比当前版本，产品分析包体积和解析峰值内存分别设置回归预算；超过预算必须通过分模块文件或懒加载解决，不能直接把全部实体状态塞入首页包。

---

## 11. 推荐版本拆分

### 0.4.1：结论纠偏

- 完整金钱来源字典。
- 异常窗口与相对低点分离。
- 重命名 `actionable` 协议。
- 拆开 `combat_or_idle`。
- 增加相应单元测试和迁移兼容。

### 0.5.0：战斗切分与事实增强

- 战斗原子标准化与去重。
- 长片段二次切分。
- 参与者状态和证据链。
- 连续可见性第一版。
- 30 场以上多版本自动分布评估。

### 0.6.0：资源状态层

- 逐波兵线状态。
- 营地状态和叠野归属。
- 动态比赛阶段。
- 大地图资源时间轴。
- 暂不输出最优路线结论。

### 0.7.0：建议层

- 安全兵线门禁。
- 漏线归因。
- 线野路线候选和资源冲突。
- 1 至 5 号位差异化建议。
- 团战责任可用性门禁。

### 后续模型版本

- 跨比赛特征库。
- 弱监督战斗分类校准。
- 路线结果排序与风险校准。
- 相似战斗和相似发育片段检索。

---

## 12. 建议立即开始的实施顺序

1. `GoldReasonCatalog` 与金额守恒测试。
2. 修正低收入窗口、`actionable` 和 `combat_or_idle` 产品协议。
3. 新建 `CombatSegmenter`，解决长片段和双地点冲突。
4. 将原始 interval 中已有的魔法、移动速度、可见性、物品冷却和技能状态接入产品索引。
5. 完成团战视野事实和职责门禁，暂不增加新的负面评分项。
6. 让 `ProductAnalysis` 接受单位状态事件，先生成兵线波次和营地事实。
7. 事实层稳定后再实现安全兵线、漏线和路线引擎。

这一顺序能先消除现有误导，再逐步增加能力，不会在缺数据时把复杂公式包装成准确分析。

---

## 13. 需要产品侧确认的决策

以下问题的研究依据和建议答案见
[`DOTA-LENS-COMBAT-FARM-RESEARCH-DECISIONS.md`](./DOTA-LENS-COMBAT-FARM-RESEARCH-DECISIONS.md)。
该文档还补充了双方至少 3 名有效参与者、爆发豁免、6/8/10 秒切分、动态职责和多尺度打钱窗口等建议。

1. 2v3 高强度战斗是否继续允许升级为团战。
2. 7 秒无主动接触是否作为长战斗切分起始阈值。
3. 低收入窗口没有异常时，是否完全隐藏，还是作为“相对低点”保留。
4. “梦游”是否只作为娱乐化页面标签，正式报告统一写“可疑空转”。
5. 辅助未插真眼是否只在隐身、高台、肉山和关键目标场景评价。
6. 路线推荐第一版是否只覆盖 1/2/3 号位，4/5 号位先做团队资源贡献。
7. 建议输出是否接受“收益范围 + 风险等级”，而不输出伪精确的死亡概率和固定多赚金额。

---

## 14. 2026-07-20 实施状态

本轮已将研究结论接入 `product-modules/2.5`，不是仅更新规则文档。

### 14.1 已完成

1. 新增补丁感知的 `GoldReasonCatalog`：
   - 保留每笔正负金钱事件的原始 `reason`；
   - 输出细分 `source`、大类 `category`、`mapped` 和分类证据；
   - 未知值固定输出 `unknown_reason_<id>`，不再并入战斗经济；
   - 对远古、召唤单位和英雄助攻进行同秒事件上下文细分；
   - 输出来源事件数、正负金额、未映射率和金额守恒校验。
2. 修正打钱诊断协议：
   - 分离 `anomalies_by_slot` 与 `relative_lows_by_slot`；
   - 新增 `observed_action`、`best_supported_option`、`matched_best_option`、
     `review_required`、`recommendation_enabled` 和 `evidence_status`；
   - 旧 `actionable` 仅保留一版兼容并标记 `actionable_deprecated=true`；
   - 将零收益窗口拆为参战、阵亡、TP/赶路、基地补给、疑似停滞和未知等行为；
   - 在兵线实体、营地存活和资源归属缺失时禁止生成确定路线。
3. 接入产品层每秒事实：
   - `move_speed`、`visible_by_team`、昼夜视野、FoW 队伍和显隐半径；
   - 技能等级、冷却、冷却总长、充能和魔耗；
   - 物品冷却、冷却总长和充能；
   - 出装结构时间线继续按物品变化去重，冷却倒计时保存在逐秒快照，避免事件列表膨胀。
4. 战斗识别升级为 `combat-rules/2.2`：
   - 团战要求双方至少 3 名有效参与者；
   - `2v2` 和 `2v3` 不升级为团战；
   - 2 至 5 秒、3v3 以上、至少 2 人阵亡且多项强证据成立时允许爆发团战豁免；
   - 无死亡团战使用更严格的投入证据门槛。
5. 新增 `combat-segmenter/2.0`：
   - 使用 6 秒软边界、8 秒默认边界和 10 秒硬边界；
   - 联合战场中心、目标集合和参与者交集识别持续追击；
   - 约 1800 世界单位作为软空间变化，约 2300 世界单位作为双战场硬分离依据；
   - 模块输出切分前后的候选数量，便于回归评估。
6. 前端已停止消费 `diagnostic.actionable`，能够展示相对低点、细分行为和新的金钱来源。

### 14.2 验证结果

- Replay Parser：49 个测试全部通过，0 失败。
- 新增覆盖：逐秒运行时状态、未知金钱来源与金额守恒、3 秒爆发团战、
  7 秒目标/中心变化切段、异常与相对低点分离。
- 前端：`node --check app.js` 通过，Vite 生产构建通过。
- 桌面端：Electron Windows x64 目录打包通过，新版解析器 JAR 已写入
  `release/win-unpacked/resources/parser/`，与源码构建产物 SHA-256 一致。
- 当前生产包仍有单个 JavaScript chunk 超过 500 kB 的构建警告，属于性能优化项，不影响本轮功能正确性。

### 14.3 仍未完成

1. `visible_by_team` 已保留，但连续敌方可见性索引和最后消失位置尚未建立；当前风险模型仍主要依赖战斗日志近窗。
2. 战斗日志原子去重、DoT/反射/幻象/召唤物完整归属尚未独立成 `CombatAtomNormalizer`。
3. 逐波兵线实体、营地存活/占用、队友资源归属和真实清线清野耗时仍缺失，因此路线推荐保持关闭。
4. 技能和物品状态已经进入快照，但沉默、眩晕、施法距离、合理目标等职责门禁尚未全部实现。
5. `GoldReasonCatalog` 已按 patch 输出 profile，当前枚举映射仍共用 Source 2 基线；发现版本差异时需要增加独立 profile。
6. 6/8/10 秒规则已有单元测试，仍需在真实比赛批次上统计过切分率、漏切分率和中心误差。

---

## 15. 2026-07-21 P0 事实层实施状态

本节替代 14.3 中关于连续视野、兵线/营地状态和职责门禁“尚未实现”的旧结论。产品协议升级为 `product-modules/2.8`，坐标协议升级为 `dota-map-coordinates/2.0` 与 `dota-map-regions/2.0`。

### 15.1 多 Patch 地图校准

- 内置 `legacy_pre_7.33`、`new_frontiers_7.33_to_7.40`、`current_7.41` 三类 profile；未知 patch 使用动态回退，不伪装为精确匹配。
- 三个 profile 分别保存三路走廊和核心地标；7.33 前后不再共用同一套路网。
- Replay 中的中立营地生成器和建筑作为动态锚点；建筑查询优先匹配同阵营真实锚点，营地查询优先使用本局 Replay 锚点。
- `map.manifest` 输出 patch family、匹配模式、静态 profile 和动态锚点数量；`map.calibration_anchors` 可供页面与 QA 直接检查。

边界：静态 profile 仍需更多版本黄金样本完成 P95 世界坐标误差验收。动态锚点优先于静态点，但没有把“实体进入 PVS”解释为地图刷新事件。

### 15.2 连续敌方可见性

- 新增 `vision.visibility_by_team`，按观察方阵营、敌方英雄和时间压缩输出连续区间。
- 证据优先级为 Replay 可见掩码、战斗日志可见事实、己方英雄几何覆盖、观察眼几何覆盖、最后可见位置投影。
- 状态固定为 `confirmed`、`probable`、`last_seen`、`unknown`；最后可见最多外推 20 秒，并随移动速度扩大不确定半径。
- `last_seen` 不输出敌人的隐藏当前坐标，只保留最后确认坐标、距最后确认时间和不确定半径。
- 打钱风险复核和地图展示均消费同一连续索引，不再从静态英雄轨迹读取战争迷雾后的真实位置。

边界：英雄和眼的几何覆盖只能标为 `probable`；高低坡、树林遮挡、烟雾、隐身和真实视野阻挡尚未进入几何模型。

### 15.3 逐波兵线与营地状态

- `farm.lane_waves` 按 30 秒理论波次聚合本局实际兵线实体，保存进入时间、单位数量、死亡/PVS 丢失和证据置信度。
- 波次状态使用 `observed_present -> observed_cleared` 或 `visibility_lost`；PVS 离开不等于死亡。
- `farm.camp_states` 以 Replay 真实中立生成器为营地锚点，按时间保存观察周期和状态迁移。
- 营地输出区分 `observed_present`、`observed_cleared`、`visibility_lost` 和压缩后的 `unknown` 区间；未知区间不补造刷新或占用事实。
- `farm.resource_state_schema` 对波次、营地、PVS 语义和限制做机器可读声明。

边界：当前可确认“看见过、清空或离开可见范围”，还不能仅靠 `unit_enter/unit_left` 精确证明刷新、拉野、叠野归属、队友资源占用或英雄真实清理耗时。因此最优路线只能展示事实候选，不得输出确定的反事实收益结论。

### 15.4 战斗职责硬门禁

- 每个技能责任候选必须同时满足：已学习、冷却/充能可用、魔法足够、英雄存活、未被眩晕或沉默、施法距离可达、存在合理敌方目标。
- 输出 `responsibility_gate.status`、证据覆盖、机会窗口秒数和阻断原因；状态只有 `passed`、`blocked`、`insufficient_evidence`。
- 只有 `passed` 且机会窗口不少于 2 秒，才允许产生“技能未交”“技能输出不足”等负面结论。
- 缺少施法距离、目标语义或连续状态时一律 `insufficient_evidence`，不能降分；所有候选采样均被明确阻断时才是 `blocked`。
- 真眼责任只在肉山等需要真眼的上下文检查，不再对每场普通战斗统一扣分。

边界：当前 Source 2 Replay 技能实体未实际输出已尝试的 `m_iCastRange/m_nCastRange/m_flCastRange`，所以真实数据的距离门禁会保持 `insufficient_evidence`。自施法、友军目标、矢量施法、全图技能、切换技能和复杂召唤物语义也需要按 Patch 的独立能力元数据；缺少这些信息时继续保持不评分，禁止使用统一默认施法距离。

### 15.5 自动验证

- Replay Parser 共 60 个测试，0 失败；新增覆盖多 Patch profile、动态锚点、PVS 与死亡区分、连续最后可见、职责门禁通过/阻断/证据不足和施法距离快照。
- 真实比赛 `8903960010` 回归生成 431 个兵线波次、22 个营地及 238 个观察周期、5489 个连续可见性区间和 42 个战斗片段。
- 真实比赛 `8904265149` 从 `.dem` 重新解析，生成 348 个兵线波次、22 个营地及 165 个观察周期、4345 个连续可见性区间和 39 个战斗片段；184 个玩家战斗责任样本中 183 个为证据不足、1 个明确阻断，门禁未通过却生成负面技能责任结论为 0。
- 同场用户可见坐标验收：10 名英雄的有效位置快照、39 个战斗落点、65 个眼位、348 个兵线波次和 22 个营地均为 0 个无效坐标；底层无效转换尝试来自未初始化或地图外实体，继续保留在诊断中但不进入地图模块。
- `.dem` 全链路同时证实 Replay 未提供施法距离字段，因此单元测试中的“门禁通过”能力只验证规则正确性，不代表真实比赛已具备该项证据。
- 新增模块采用拆分 gzip 存储；不能用完整 pretty JSON 的磁盘体积代表桌面端实际分析包体积。

### 15.6 下一步门槛

1. 对 7.33 前、7.33 至 7.40、7.41 各选真实 Replay，建立塔、营地、肉山坑和路线锚点误差报告。
2. 接入树林、高低坡、烟雾、隐身和显隐技能状态，把几何 `probable` 逐步升级为规则可证实状态。
3. 解析营地出生/死亡与兵线逐单位死亡归属，补齐拉野、叠野、漏线和队友收线事实。
4. 从 Dota 客户端 VPK 或可版本化的官方游戏数据构建技能施法距离、目标类型和复杂施法元数据，再扩展 1 至 5 号位的差异化职责评分。
