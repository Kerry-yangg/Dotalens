# Dota Lens 0.4.5 本地 EXE 全流程验收报告

> 验收日期：2026-07-26  
> 结论：本地工程基线通过，暂不发布 Git  
> 桌面端：`0.4.5`  
> Parser API：`1.6.0`

## 1. 候选安装包

| 项目 | 结果 |
| --- | --- |
| 文件 | `release/Dota-Lens-Setup-0.4.5-x64.exe` |
| 大小 | `158,312,372` bytes |
| SHA-256 | `1976A618E8F78EEE94B972A765DAFEC050598F6488D9BC9B0F09224236D393DE` |
| 内置 Java | `21.0.11` |
| 内置 Parser API | `1.6.0` |

## 2. 自动化测试

| 测试集 | 结果 |
| --- | --- |
| Parser | `127/127` 通过，真实 Replay 离线 E2E 未跳过 |
| 前端 | `65/65` 通过 |
| 桌面端 | `3/3` 通过 |
| Vite 生产构建 | 通过 |
| PowerShell 冒烟脚本语法 | 通过 |

真实离线 E2E 使用 `8894766243.dem` 和录像内账号 `137129583`。OpenDota
地址固定为不可连接的 `127.0.0.1:1`，仍成功生成十名玩家、反补、身份归属和完整分析。

## 3. 安装包全流程

### 3.1 未压缩 Replay

样本：`8894766243.dem`

- 安装、主进程启动、内置 JRE、Parser `1.6.0` 和隔离数据目录均通过。
- 解析任务进入实际工作阶段后可以撤销，状态为 `canceled`，无 `.part` 残留。
- 完整解析得到 `267,683` 条事件，耗时约 `35.0s`。
- 账号 `137129583` 自动匹配到 `player_slot 128`。
- 人工切换到 `player_slot 0` 后，冷启动仍为 `manual_selected`。
- Patch 为 `7.41d`，来源状态为 `inferred`，三个 Patch 门禁字段完整。
- 静默卸载成功，应用文件被移除。
- 结果：`26/26` 检查通过。

证据：`tools/runtime/exe-smoke-0.4.5/smoke-report.json`

### 3.2 压缩 Replay

样本：`8909845275.dem.bz2`，大小约 `39.7 MB`

- 完整解析得到 `190,658` 条事件，耗时约 `27.3s`。
- 账号 `139766850` 自动匹配到 `player_slot 0`。
- 人工切换到 `player_slot 1` 后，冷启动仍能恢复。
- 安装、撤销、解析、Patch、重启和卸载共 `26/26` 检查通过。

证据：`tools/runtime/exe-smoke-0.4.5-bz2/smoke-report.json`

### 3.3 账号不在录像

样本：`8894766243.dem`，请求账号 `99735754`

- 初次解析返回 `manual_required` 和 `requested_account_not_in_replay`。
- 未解析身份时 `selected_player_slot` 为空，没有默认选择第一位玩家。
- 人工选择 `player_slot 0` 后返回 `manual_selected`。
- Parser 重启后仍保持同一槽位。

证据：`tools/runtime/0.4.5-manual-subject-smoke.json`

## 4. 可信导入负例

把内部比赛 ID 为 `8894766243` 的 Replay 按 `8894766242` 导入后：

- 任务返回 `replay_match_id_mismatch`。
- 错误上下文同时给出声明 ID 和内部 ID。
- 错误缓存副本被移除。
- 用户原始 Replay 的 SHA-256 前后保持一致。

证据：`tools/runtime/0.4.5-replay-identity-smoke.json`

## 5. 布局验收

最终候选 EXE 覆盖玩家评分四档视口，以及发育、打钱、团战、视野和设置页：

- 共 `9/9` 个场景通过。
- 英雄图片损坏数为 `0`。
- 非预期可见横向溢出为 `0`。
- `1024x720` 发育复盘地图由 `126px` 修复为 `248px`。
- 小高度窗口隐藏资源倒计时和折叠单位栏，把空间优先留给地图。
- 小地图隐藏易堆叠文字；放大地图后仍显示完整信息。

证据：`qa/0.4.5-exe-layout/layout-smoke-report.json`

## 6. 剩余限制

- 安装包没有商业代码签名，应用仍使用 Electron 默认图标。
- 当前显示器无法提供完整 `1920x1080` 客户区，最大场景实际为 `1693x982`。
- 主应用和图表运行时仍有大于 `500 kB` 的构建告警，归入后续首屏性能优化。
- OpenDota 首次离线且没有账号缓存时，在线比赛列表仍不可用；本地 Replay 不受影响。
- 当前工作区包含 0.5.0 预览功能且尚未整理为公开提交，因此本结论只代表本地候选基线通过。

## 7. 基线决定

`0.4.5` 的本地 Replay 可信导入和 EXE 工程门槛已经关闭。按用户决定，本轮不创建
Git 提交、标签、远端推送或 GitHub Release。后续直接在当前开发分支继续
`0.5.0` 玩家报告产品化，随后处理 JSON 体积和首屏性能。
