# Dota Lens 战斗与打钱判断逻辑评审稿

版本：当前程序实现快照（桌面端 0.4.0）  
日期：2026-07-19  
主要实现：`tools/replay-parser/src/main/java/opendota/ProductAnalysis.java`

## 1. 文档目的

这份文档只描述当前程序实际执行的规则，不把路线图里的未来方案写成已经完成的功能。

请重点检查三类内容：

1. 类型定义是否符合真实 Dota 语义。
2. 当前阈值是否过严或过松。
3. 哪些额外上下文必须加入，才能给玩家下结论。

### 1.1 证据等级

| 等级 | 含义 | 例子 |
|---|---|---|
| 事实 | Replay 中存在直接事件 | 英雄伤害、死亡、控制、金钱、单位击杀、插眼、TP 引导 |
| 派生规则 | 由事实按固定公式推导 | 战斗类型、风险分、线野循环、团战重要性 |
| 禁止判断 | 关键字段缺失，不输出确定结论 | 未知兵线存量时判断“漏了几只兵”、未知营地存活时规划最优线野路线 |

地图距离统一使用归一化地图百分比坐标，地图横纵范围均为 `0..100`。例如距离 `12.5` 不是 12.5 世界单位，而是约 12.5% 地图跨度。

---

## 2. 战斗识别总流程

```text
敌对英雄伤害 / 敌对英雄控制 / 英雄死亡
                    ↓
              时空事件聚类
                    ↓
           有效战斗候选门槛
                    ↓
        二次合并相邻的同一轮交战
                    ↓
      计算接触时间、峰值、地点、参与者
                    ↓
       五分类硬门槛 + 候选解释分
                    ↓
   上下文标签、战斗阶段、重要性、个人贡献
```

## 3. 战斗候选如何形成

### 3.1 可以启动战斗聚类的原子事件

当前只有以下三类事件可以启动或延长战斗：

| 原子 | 条件 | 位置取值 |
|---|---|---|
| 英雄伤害 | 攻击者和目标都是有玩家槽位的敌对英雄 | 优先取目标英雄位置，缺失时取攻击者位置 |
| 英雄控制 | 施法者和目标是敌对英雄 | 同上 |
| 英雄死亡 | 目标是英雄且能映射玩家槽位 | 同上 |

治疗、技能使用、物品使用不会单独创建战斗候选。它们只会在战斗候选形成后，被附加到已有战斗中。

这意味着：纯逼退、纯威慑、只开技能但没有形成伤害或控制的行为，目前不会被识别成战斗。

### 3.2 第一阶段时空聚类

一个新事件加入已有战斗簇，需要满足：

1. 与该簇最后事件间隔不超过 `6 秒`。
2. 双方都有位置时，新事件距当前战斗中心不超过 `12.5%` 地图跨度。
3. 任一侧缺少位置时，必须与已有簇共享参与者，且时间间隔不超过 `3 秒`。
4. 同时符合多个簇时，选择 `空间距离 + 时间间隔秒数 × 1.5` 最小的簇。

### 3.3 最低有效战斗门槛

聚类后先剔除环境噪声。一个簇必须双方都有英雄参与，并满足任意一项：

1. 至少有 `1` 次英雄死亡。
2. 总英雄伤害至少 `600`。
3. 总英雄伤害至少 `300`，且控制至少 `1.0 秒`。
4. 总英雄伤害至少 `450`，且战斗原子事件至少 `6` 条。

低于门槛的普通线上点人、少量 DoT 和偶发控制不会进入战斗列表。

### 3.4 第二阶段相邻簇合并

两个相邻簇只有同时满足以下条件才合并：

1. 两簇间隔不超过 `5 秒`。
2. 至少共享 `2` 名参与者。
3. 共享人数占较小簇参与人数至少 `50%`。
4. 两簇都有位置时，中心距离不超过 `21.5%` 地图跨度。
5. 缺少中心位置时，条件收紧为共享至少 `3` 人且间隔不超过 `2 秒`。

当前已知风险：二次合并可以递归发生，长追击可能把多轮接触串成一场 `79 至 123 秒` 的战斗。尚未加入“连续 6 至 10 秒无接触、中心跃迁、交互图断裂、主要目标切换”的二次切分。

---

## 4. 战斗时间、地点与参与者

### 4.1 时间定义

| 字段 | 当前定义 | 是否参与分类 |
|---|---|---|
| `review_start` | 第一条有效接触前 `10 秒` | 否，只用于复盘准备阶段 |
| `contact_start` | 簇内第一条伤害、控制或死亡 | 是 |
| `contact_end` | 簇内最后一条伤害、控制或死亡 | 是 |
| 展示结束 | `contact_end + 4 秒` | 否 |
| 转化窗口 | `contact_end + 30 秒` | 只用于目标转化与重要性 |

战前 10 秒中的技能和路过英雄不会抬高团战人数、伤害、技能数和持续时间。

### 4.2 主战场位置

1. 在战斗簇内滑动 `3 秒` 窗口，寻找事件强度最高的峰值。
2. 取峰值前 `1 秒` 到峰值后 `1 秒` 的位置事件。
3. 少于 2 个位置点时，退回使用整个战斗簇的位置点。
4. 用加权几何中位数求中心，不用所有英雄位置的普通平均值。
5. 剔除距初始中心超过 `15.5%` 的空间离群点。

位置权重：

| 事件 | 权重 |
|---|---|
| 英雄死亡 | `10` |
| 控制 | `clamp(控制秒数 × 1.8, 2.5, 6)` |
| 伤害 | `clamp(伤害 / 140, 1, 7)` |

战场散布半径：`clamp(加权标准差 × 1.35 + 2, 3, 18)`。

位置置信度：`clamp(90 - 散布半径 × 2.8 + min(22, 有效位置点数 × 2), 35, 96)`。

### 4.3 有效参与者

只有在正式接触窗口内发生以下有效行为，才计为 `active participant`：

1. 造成或承受敌方英雄伤害。
2. 造成或承受敌方控制。
3. 击杀或死亡。
4. 造成或接受非生命恢复、非吸血治疗。
5. 在已有战斗参与者链路内使用技能或物品。

只是在峰值时距离战场中心 `13.5%` 内但没有有效行为的英雄，只进入 `nearby_slots`，不会增加团战人数。

当前限制：只使用了技能但未进入初始伤害、控制、死亡参与者链路的英雄，可能漏算；纯走位威慑和未命中的技能也不会成为有效参与者。

---

## 5. 战斗分类使用的核心特征

| 特征 | 计算方式 |
|---|---|
| 有效人数 | 正式接触窗口内的 `active participant` |
| 双方人数平衡度 | `较少方人数 / 较多方人数` |
| 总伤害 | 双方英雄对英雄伤害之和 |
| 双方互换率 | `较低一方伤害 / 较高一方伤害` |
| 目标集中度 | `单一目标承受的最高伤害 / 总伤害` |
| 生命池伤害比 | `总伤害 / 开战时所有有效参与者最大生命值之和` |
| 交互图密度 | 唯一“攻击/控制者 → 目标”边数除以 `天辉人数 × 夜魇人数 × 2` |
| 技能数 | 正式接触窗口内的技能使用次数 |
| 物品数 | 正式接触窗口内的非 TP 物品使用次数 |
| 控制秒数 | 正式接触窗口内控制时长之和 |
| 持续时间 | 第一条到最后一条战斗原子之间的秒数，至少为 1 |
| 兵线上下文 | 主战场中心是否落在上、中、下路区域多边形中 |

### 5.1 战斗强度分

```text
强度 = min(100,
    min(40, 总伤害 / 55)
  + min(30, 死亡数 × 15)
  + min(15, 控制秒数 × 3)
  + min(12, 技能与物品使用数 × 1.5)
  + min(8, 持续时间 / 2)
)
```

### 5.2 高投入证据

每满足一项记 1 条：

- 总伤害至少 `2400`。
- 技能加非 TP 物品使用至少 `8` 次。
- 生命池伤害比至少 `0.70`。
- 控制至少 `5.0 秒`。

### 5.3 团战证据

每满足一项记 1 条：

- 死亡至少 `2` 人。
- 总伤害至少 `2400`。
- 技能加非 TP 物品使用至少 `8` 次。
- 有效参与者至少 `7` 人。
- 生命池伤害比至少 `0.70`。
- 交互图密度至少 `0.32`。

---

## 6. 五类战斗的硬判断顺序

最终类型不是简单取候选分最高值，而是按以下顺序走硬门槛。前面的规则一旦命中，后面不再执行。

### 6.1 无死亡事件

#### A. 高投入无死亡小规模冲突 `skirmish`

必须同时满足：

1. 死亡数为 `0`。
2. 有效参与者至少 `4` 人。
3. 双方至少各 `2` 人。
4. 强度至少 `50`。
5. 以下任一成立：总伤害至少 `1600`、生命池伤害比至少 `0.45`、控制至少 `2 秒`。

语义：多人投入较高，但最终没有减员，例如无死亡逼退或抢目标时的高强度接触。

#### B. 线上换血 `lane_trade`

没有达到高投入无死亡冲突，并同时满足：

1. 主战场在兵线区域。
2. 双方都造成了英雄伤害。
3. 双方伤害互换率至少 `0.20`。

语义：双方都有实际回击的线上换血。河道和野区发生同样伤害不会标记为“线上换血”。

#### C. 消耗 `harass`

无死亡且未达到以上两类时，归为消耗。

- 在兵线区域：低投入线上消耗。
- 不在兵线区域：非兵线试探或远程 poke。

### 6.2 有死亡事件

#### D. 固定 2v2 小规模冲突 `skirmish`

只要有效人数恰好为天辉 2 人、夜魇 2 人，就固定为小规模冲突。即使伤害、技能数和死亡数很高，也不能升级为团战。

这是当前最高优先级的有死亡分类约束。

#### E. 团战 `teamfight`

非 2v2，并且必须同时满足：

1. 有效参与者至少 `5` 人。
2. 双方至少各 `2` 人。
3. 正式有效接触至少 `5 秒`。
4. 双方人数平衡度至少 `0.40`。
5. 战斗强度至少 `60`。
6. 团战证据至少 `2` 条。
7. 高投入证据至少 `1` 条。

由此可见：

- `2v3` 可以成为团战，但必须通过全部强度门槛。
- `1v4` 不能成为团战，因为人数平衡和双方至少 2 人不满足。
- `3v3` 不会自动成为团战，仍需持续时间、强度和证据。
- `5v5` 也不会自动成为团战，短暂低强度接触仍可能是小规模冲突。

#### F. 抓单/单杀 `pickoff`

团战门槛未通过、恰好死亡 `1` 人，并满足任一项：

1. 命中“孤立目标抓单”条件。
2. 有效参与者不超过 `3` 人。
3. 目标集中度至少 `0.68`。

“孤立目标抓单”条件：

1. 第一名死者所在一方只有 `1` 名有效参与者。
2. 进攻方至少有 `2` 名有效参与者。
3. 目标集中度至少 `0.50`。

1v1 且发生死亡会附加 `solo_kill` 标签，也归入 `pickoff`。

#### G. 其它有死亡战斗 `skirmish`

有死亡，但不是固定 2v2、未通过团战门槛，也不符合单次抓单条件时，归为小规模冲突。

典型例子：2v3 低强度交战、多人短暂接触只死一人但伤害较分散、多人交战死亡两人但持续时间不足 5 秒。

---

## 7. 候选解释分与置信度

程序同时输出五类候选分，供页面解释，不直接推翻第 6 节的硬门槛。

```text
消耗分 = 48 + (1 - 互换率) × 28 + 小于等于3人奖励10 - 强度惩罚
换血分 = 45 + 互换率 × 42 + 小于等于4人奖励8 - 过高强度惩罚
抓单分 = 32 + 目标集中度 × 34 + 抓单奖励20 + 单杀奖励15 - 多人惩罚
小规模分 = 18 + 人数 × 6 + 平衡度 × 20 + 强度项 + 2v2奖励20 - 7人以上惩罚12
团战分 = 12 + 人数 × 6 + 平衡度 × 18 + 强度项 + 团战证据数 × 5
```

置信度基于“最终类型分与第二高分的差距”及位置、生命池、交互边和伤害证据计算，范围 `38..96`。

- 固定 2v2 的规则置信度至少 `90`。
- 通过团战硬门槛时，置信度会按人数、证据数和强度提高。
- 当前字段明确标记为 `uncalibrated`，它表示规则证据完整度，不代表人工真值准确率，也不能解释为“90% 概率确实是团战”。

---

## 8. 与主类型正交的特殊标签

这些标签不会改变五类主类型。

| 标签/上下文 | 当前判断 |
|---|---|
| `lane_context` | 战斗中心位于兵线区域 |
| `gank` | 第一死者为本方唯一有效参与者，敌方至少 2 人，目标集中度至少 0.50 |
| `solo_kill` | 双方各 1 名有效参与者且有死亡 |
| `tp_support` | TP 确认落在战场附近，之后在战斗窗口内出现首次战斗行动 |
| `tp_arrival` | TP 落在附近，但未观察到后续战斗行动 |
| `chase` | 战斗持续至少 18 秒且空间散布半径至少 6% |
| `high_intensity` | 强度至少 80 |

### 8.1 TP 支援

当前确认链：

1. 复盘开始到正式接触结束之间使用 TP。
2. 优先读取 TP 修饰符判断引导开始、完成或打断。
3. 无完整修饰符时，用施法后 `2 至 8 秒` 的逐秒位置快照寻找至少 `6%` 地图跨度的位置跳变。
4. 落点距战场中心不超过 `max(17%, 战斗附加半径 + 5%)`。
5. 从落地到战斗结束后 4 秒内，玩家出现伤害、控制、治疗、死亡关联或非 TP 技能/物品行为。

只有“确认落地附近 + 出现战斗行为”才叫 `tp_support`。打断 TP、落地不可见、落在别处或只到场未行动不会标记为确认支援。

当前不评价“为什么没 TP”：因为尚未完整核对 TP 库存、冷却、魔法、建筑可用性、玩家受控/死亡状态、距离和战斗是否仍可挽救。

### 8.2 越塔与塔区战斗

1. 找距战斗中心最近的塔，超过 `12%` 不建立塔区上下文。
2. 根据该塔此前是否有摧毁事件判断开战时是否存活。
3. 只有同时满足以下条件才是 `confirmed_dive`：
   - 塔开战时存活；
   - 战斗中心距塔不超过 `8.5%`；
   - 进攻方对英雄造成过伤害；
   - Replay 中存在该塔实际攻击进攻方英雄的伤害事件。
4. 有进攻方英雄伤害但没有塔伤，只标记 `tower_zone_fight`，不下“越塔”结论。
5. 战后 20 秒内塔被摧毁，记为塔转化成功。

### 8.3 肉山相关战斗

1. 对肉山的英雄伤害间隔超过 `20 秒`，开始新一轮肉山尝试。
2. 肉山尝试前 10 秒至结束后 5 秒，坑附近 `16%` 内的敌对英雄交战视为争夺证据。
3. 双方都打过肉山，或非主输出方有坑内交战参与者，记为有争夺。
4. 击杀方不是主输出方，且主输出方贡献至少 `500` 伤害并占总伤害至少 `55%`，记为抢肉山。

肉山尝试类型：

| 类型 | 当前判断 |
|---|---|
| 试探/假打 | 未击杀且观测伤害小于 800 |
| 放弃尝试 | 未击杀且观测伤害至少 800 |
| 无争夺肉山 | 有击杀且无争夺 |
| 有争夺肉山 | 有击杀、有争夺，但坑内交战参与者少于 5 |
| 肉山团 | 有击杀、有争夺，坑内交战参与者至少 5 |
| 抢肉山 | 满足上面的主输出方与击杀方反转条件 |

仅仅在静态肉山坑附近打架但没有肉山伤害，只标记“肉山坑附近战斗”，不会伪造成肉山团。

---

## 9. 战斗阶段与重要性

### 9.1 五阶段

| 阶段 | 当前边界 |
|---|---|
| 准备 `setup` | 正式接触前 10 秒 |
| 先手 `initiation` | 第一条接触到对方第一次有效回应；没有回应时最多取前 2 秒 |
| 正面交战 `clash` | 先手结束到峰值窗口结束后 1 秒 |
| 收尾 `cleanup` | 峰值结束到最后有效接触 |
| 转化 `conversion` | 接触结束后 30 秒 |

### 9.2 重要性与战斗类型分开

一场 2v2 永远可以保持“小规模冲突”，但仍可能因为越塔反打、经济反转或肉山转化成为关键战斗。

重要性总分 `0..100`：

| 分项 | 上限 | 主要因素 |
|---|---:|---|
| 战果与局势摆动 | 30 | 死亡数、双方死亡差、战后净值摆动、经济领先翻转 |
| 目标转化 | 25 | 肉山18、盾14、兵营16、基地20、塔8、信使2、其它4 |
| 资源投入 | 20 | 参与人数、技能、物品、控制、死亡 |
| 支援响应 | 10 | 确认 TP 支援每次4分，只到场每次2分 |
| 战略上下文 | 15 | 基地、肉山、推塔、高强度、20分钟后 |

- `< 40`：常规。
- `40..69`：重要。
- `>= 70`：关键。

该分数同样是未校准临时规则，不是胜负贡献概率。

---

## 10. 团战视野和个人责任

### 10.1 团战视野

对每一方分别计算：

1. 战斗中有多少英雄伤害事件被该方 CombatLog 标记为可见。
2. `可见伤害事件 / 全部伤害事件` 得到战斗日志可见率。
3. 战场中心 `16%` 范围内有哪些存续中的真假眼。
4. 眼的几何视野半径是否覆盖战场中心。
5. 准备窗口附近新插的真假眼数量。

当前限制：这是“伤害事件可见率 + 眼位几何”的混合证据，不是每一秒完整战争迷雾重建。

### 10.2 核心与辅助的临时识别

在比赛第 `min(10分钟, 比赛时长)` 的快照中，按队内净值排序：

- 前 3 名视为 `core`。
- 后 2 名视为 `support`。

缺少净值时，用补刀数 × 45 代替。当前不会细分 1、2、3、4、5 号位，也不会处理换位、游走核心或特殊阵容。

### 10.3 核心责任分

```text
28
+ min(36, 团队伤害占比百分数)
+ min(12, 技能次数 × 3)
+ min(10, 对死亡目标伤害占个人伤害比例 × 10)
+ 战场存在率 × 0.14
- 本人死亡数 × 6
```

问题标签：

- 团队伤害占比低于 `12%`：输出不足。
- 技能次数为 `0`：没有技能输出。
- 战场存在率低于 `45%`：迟到或缺席。
- 战斗有减员但该玩家对所有死亡目标伤害为 0：没有参与击杀转化。

### 10.4 辅助责任分

```text
30
+ min(22, 技能次数 × 4)
+ min(15, 控制秒数 × 2.5)
+ min(10, 治疗量 / 250)
+ min(10, 战前附近真假眼数 × 5)
+ min(8, 物品使用数 × 2)
+ 战场存在率 × 0.10
- 本人死亡数 × 5
```

问题标签：

- 技能次数为 `0`：没有施法。
- 控制和治疗都为 0，且团队伤害占比低于 `8%`：功能输出不足。
- 战前附近没有真眼：未做真眼准备。
- 战场存在率低于 `45%`：迟到或缺席。

责任状态：`>=72 正常`、`48..71 观察`、`<48 问题`。

当前这套“内鬼判断”只能作为线索，不能作为最终责任判定，主要缺口包括：技能是否在冷却、魔法是否足够、是否被沉默或眩晕、关键技能是否命中正确目标、英雄职责差异、买活、BKB 时机、救人技能是否有可救目标，以及该场战斗是否真的需要辅助插真眼。

---

## 11. 打钱数据如何归类

### 11.1 金钱事件来源

当前先保留 `gold_reason = 11..21` 的正向金钱事件，再按以下临时规则分类：

| `gold_reason` | 当前来源 |
|---:|---|
| 13 | 兵线 `lane` |
| 14 | 中立生物 `neutral` |
| 其它 11..21 | 战斗/其它 `combat` |

这是当前最明显的待修规则。塔、英雄击杀、助攻、肉山、赏金符、建筑等来源可能被合并到 `combat`，所以现在的“战斗经济”并不等于纯击杀收益。

金钱位置优先使用事件自身世界坐标；事件没有坐标时，回退到该玩家最接近该秒的英雄位置快照。

### 11.2 击杀单位类型

单位死亡记录按目标名称分为：

| 类型 | 当前规则 |
|---|---|
| 英雄 | 目标标记为英雄 |
| 小兵 | 名称包含天辉或夜魇小兵 |
| 普通中立 | 名称包含 `neutral` 且不是远古营地 |
| 远古 | 名称包含 `neutral` 且 `neutralCampType > 0` |
| 防御塔/建筑 | 名称包含 tower、fort 或 rax |
| 信使 | 名称包含 courier |
| 肉山 | 名称包含 roshan |
| 假眼 | 名称包含 observer ward |
| 死灵书单位 | 名称包含 necronomicon 或 nec_ |
| 其它 | 未命中以上规则 |

当前没有把真眼击杀、召唤物细类、旗手、攻城车和不同中立营地分别展示。

---

## 12. 打钱热力图与线野循环

### 12.1 打钱热力图

1. 使用所有正向资源金钱事件。
2. 每 `5 分钟` 一个时间段。
3. 按兵线、中立、战斗/其它三种来源分层。
4. 地图划分成 `10 × 10` 网格。
5. 每格统计总金钱、事件数，并用金钱加权位置作为热区中心。

热力图能回答“金钱事件主要发生在哪里”，但当前不能准确回答“这个野点理论上应有多少钱”或“这波兵漏了多少钱”。

### 12.2 已观察到的线野双收

当前只承认 Replay 中已经发生的“线 → 野 → 同一路线”事实链，不根据刷新时钟凭空推荐线野双收。

必须同时满足：

1. 只分析前 `20 分钟`。
2. 找到一次中立金钱事件。
3. 该事件前 `25 秒` 内有兵线金钱事件。
4. 该事件后 `42 秒` 内有兵线金钱事件。
5. 前后两次兵线事件位于同一条线上。
6. 中立金钱事件位置属于野区。
7. 整段内兵线金钱、中立金钱和中立/远古击杀数都大于 0。
8. 与上一条已识别循环不重叠。

当前语义是“确认玩家确实完成过一次线野线循环”，不是“证明这是理论最优路线”。

### 12.3 拉野/叠野

当逐秒快照中的 `campsStacked` 增加时，输出一次已确认叠野事件：

- 增加 1：单营叠野。
- 同一快照增量至少 2：标记双叠野。
- 同时输出 `creepsStacked` 的增量。

当前无法确定具体叠了哪个营地、拉野的精确秒数、该营是否随后被自己或队友清理，也不能计算让给核心后的团队净收益。

---

## 13. 低收入窗口和打钱行为类型

### 13.1 分析范围与窗口

- 只分析前 `20 分钟`。
- 每 `30 秒` 一个窗口。
- 玩家死亡或没有位置快照的窗口跳过。
- 分别统计兵线金钱、中立金钱、战斗/其它金钱、小兵击杀、中立/远古击杀和叠野增量。

阶段标签当前为：

| 时间 | 标签 |
|---|---|
| 0 至 7:29 | 对线 `laning` |
| 7:30 至 14:59 | 扩张 `expansion` |
| 15:00 至 20:00 | 魔方前 `pre_tormentor` |

这套阶段切分目前没有使用用户提出的 10 分钟对线检查和 20 分钟转入发育/频繁冲突节点，需要重新讨论。

### 13.2 选择需要复核的低收入窗口

1. 计算该玩家所有有效 30 秒窗口总收入的中位数。
2. 低收入阈值为 `max(35, 中位数 × 0.62)`。
3. 从 1:00 以后选择收入不高于阈值的窗口。
4. 先取收入最低的 10 个，再按时间排序展示。
5. 如果没有任何窗口低于阈值，仍会展示收入最低的 5 个窗口。

第 5 条意味着当前页面展示的是“最低收入复核窗口”，不一定全部都是异常或错误窗口。UI 不应把它们直接写成“必然漏钱”。

### 13.3 实际行为类型

一个窗口按已获得的金钱来源归类：

| 类型 | 当前判断 |
|---|---|
| 线野双收 `lane_jungle_cycle` | 窗口与已观察到的线野线循环重叠 |
| 战斗或空窗 `combat_or_idle` | 总收入为 0，或战斗/其它金钱高于兵线和中立金钱 |
| 收线 `collect_lane` | 兵线金钱同时不低于中立和战斗/其它金钱 |
| 留野 `hold_jungle` | 中立金钱不低于战斗/其它金钱，且没有被判为收线 |

当前 `combat_or_idle` 把“参战”和“真正闲逛”合在一起，不能仅凭这个类型指责玩家梦游。

---

## 14. 当前路线候选与评分

### 14.1 兵线候选 `collect_lane`

只有具备兵线证据才生成：

1. 先用 0:45 至最多 7:00 的位置样本推断分路；前 5 分钟的位置样本权重为 2，之后为 1。
2. 当前 30 秒窗口的中点位置仍在该玩家推断分路上。
3. 窗口前后 30 秒内存在该玩家兵线金钱或小兵击杀。

预期收入取 `max(本窗口实际兵线金钱, 该玩家正兵线收入窗口中位数, 45)`。

当前不检查这条线此刻是否真有兵、兵线距离、塔状态、敌方绕后路线、队友是否已在收线，所以它不是严格意义上的“安全兵线机会”。

### 14.2 野区候选 `hold_jungle`

只有具备野区证据才生成：

1. 当前窗口中点位于野区。
2. 窗口前后 45 秒内存在中立金钱、普通中立击杀或远古击杀。

预期收入取 `max(本窗口实际中立金钱, 该玩家正中立收入窗口中位数, 35)`。

当前不检查营地是否存活、营地类型、英雄清野速度、被队友预占、敌方入侵概率和下一波刷新是否来得及。

### 14.3 线野双收候选 `lane_jungle_cycle`

只有第 12.2 节的完整事实链已经发生，才生成候选。缺少兵线位置、营地存活和英雄清理时间时，程序明确阻止凭刷新时间推荐线野双收。

### 14.4 当前风险分

敌方“可见”只来源于最近 `12 秒` 的英雄伤害事件，且该伤害事件被本方 CombatLog 标记可见。它不是完整敌方位置快照。

当前位置 `24%` 范围内存续中的己方假眼会降低风险；真眼当前不降低打钱风险。

```text
基础风险 = clamp(
    20
  + 未知敌人数量 × 12
  - 最近可见敌人数量 × 2
  - min(12, 附近己方假眼数 × 3),
  8, 94
)

15分钟后的兵线风险 = min(96, 基础风险 + 8)
野区风险 = max(8, 基础风险 - 12)
```

这个模型目前存在明显偏置：默认野区比兵线安全 12 分，而且不知道敌方英雄是谁、最后出现位置、移动速度、是否有跳刀/隐身、己方塔和兵线提供的视野，也没有读取完整战争迷雾。

### 14.5 候选评分

```text
候选分 = 预期金钱 - 风险 × 1.35 - 路程秒数 × 1.8
```

- 收线与留野的路程当前固定为 `0`。
- 已观察线野循环的路程取整段循环持续时间。
- 兵线机会倒计时为 `max(12, 42 - 当前秒数 % 30)`。
- 野点机会倒计时为 `max(8, 60 - 当前秒数 % 60)`。

倒计时只是刷新节奏启发式，不代表地图上真实波次到达时间或营地当前仍存活。

### 14.6 最终结果类型

| 结果 | 当前含义 |
|---|---|
| `correct` | 观测到的实际行为类型与当前最高分候选一致 |
| `review` | 有候选，但实际行为与最高分候选不同 |
| `evidence_gap` | 没有足够兵线、野区或线野循环证据生成候选 |

代码字段 `actionable=true` 当前实际表示“玩家行为与最高分候选一致”，更接近“已验证执行正确”，不是“需要立刻改正”。这个字段名容易误导，建议后续改为 `matched_best_observed_candidate`。

候选不一致时：

- 最佳候选是兵线：输出“复核附近兵线”。
- 最佳候选是野区：输出“复核当前野区”。
- 没有候选：输出“证据不足”。

当前打钱诊断置信度最高只有 `72`，刻意不把这些启发式包装成确定事实。

---

## 15. 当前明确不能可靠判断的内容

| 用户想看的结论 | 当前缺失数据/模型 | 现状 |
|---|---|---|
| 某波安全兵线应该去收 | 每只小兵位置与存活、波次到达、敌方完整位置/可见性、塔状态、路线时间 | 不能可靠判断 |
| 因刷野漏了几只兵、多少钱和经验 | 兵线单位连续状态、归属和死亡去向 | 不能可靠判断 |
| 当前营地是否能刷 | 每个营地存活、营地类型、刷新被阻挡 | 不能可靠判断 |
| 最优线野双收路线 | 兵线状态、营地状态、英雄清理耗时、移动时间、队友资源占用 | 只确认已经发生的循环 |
| 是否应该拉双野 | 具体营地、单位仇恨、刷新前状态和清理归属 | 只确认叠野计数增加 |
| 玩家是在打架还是梦游 | 战斗目标、队伍指令、路线意图、资源等待原因 | 当前合并为 `combat_or_idle` |
| 某英雄此刻去线上的死亡概率 | 完整战争迷雾、敌方技能和物品、TP/CD、路线可达性 | 当前只有粗风险分 |
| 辅助为什么没放技能/物品 | 库存、冷却、魔法、沉默/控制、施法距离、目标可救性 | 只能给输出线索 |
| 某场一定是团战 | 人工真值与多版本校准集 | 已有规则分类，但准确率未评测 |

---

## 16. 请你重点补充或拍板的规则

这些问题的资料检索、逐项建议和初始工程阈值已经整理到
[`DOTA-LENS-COMBAT-FARM-RESEARCH-DECISIONS.md`](./DOTA-LENS-COMBAT-FARM-RESEARCH-DECISIONS.md)。
研究稿是建议决策，正式进入实现前仍以产品确认结果为准。

### 16.1 战斗类型

1. `2v3` 是否允许在极高强度时叫团战，还是统一保持“小规模冲突”？当前允许通过硬门槛后成为团战。
2. 无死亡 `3v3/4v4` 抢盾、逼退、逼买活等，是否需要独立类型，还是保持“小规模冲突 + 目标标签”？
3. 团战最少 5 人是否合理，是否应按时间阶段变化，例如 10 分钟前更严格、20 分钟后保持 5 人？
4. 5 秒有效接触是否合理？爆发阵容 3 秒击杀两人是否应按结果放宽持续时间门槛？
5. 当前 2v2 永不升级团战是否继续保持，包括肉山、越塔、高地和双买活场景？当前通过“重要性”表达关键程度。
6. 抓单是否只看第一死者一方仅 1 人，还是要加入开战前 10 秒的空间孤立程度、烟雾、先手和 TP 赶到顺序？
7. 什么条件下应把长追击拆成两场：无接触 6 秒、8 秒还是 10 秒？中心跃迁多少世界单位才算新战场？

### 16.2 团战责任

1. 核心是否应按 1/2/3 号位分别设责任，而不是统一按伤害占比？
2. 辅助是否每场战斗都应该检查真眼，还是只在有隐身英雄、阴影区、高台、肉山和关键目标战时检查？
3. “技能没交”必须先满足哪些门槛：技能已学习、CD 完成、魔法足够、未被控、在施法距离、存在合理目标？
4. C 位的有效贡献是否应加入建筑伤害、承伤换技能、控制关键目标、存活输出时间和 BKB 有效时间？
5. 辅助贡献是否应加入救人技能价值、先手/反手命中、驱散、挡技能、视野发现和破雾？
6. 战场存在率低于 45% 是否足以判迟到，还是要按英雄位置、TP 能力和队伍先手责任区别判断？

### 16.3 打钱分析

1. 低收入复核窗口用 30 秒是否合适，还是前 10 分钟用每波兵线 30 秒、10 至 20 分钟用 60 秒循环？
2. “安全兵线”应满足哪些硬条件，例如最近可见敌人数量、敌方最后消失时间、己方塔、眼位覆盖、逃生技能、TP 支援和兵线深入程度？
3. 风险是否要按敌方英雄威胁加权：跳刀先手、隐身、全图技能、控制链、爆发伤害和已知关键物品？
4. 1/2/3/4/5 号位的资源优先级是否需要不同模型？例如辅助不能因为没有抢核心安全线而被判低效。
5. 对线期到底截至 10、12、15 还是由塔状态、英雄分布和经济来源动态判断？
6. 线野双收应如何按英雄能力分类：清线时间、清野时间、位移、续航、关键技能 CD、幻象或召唤物？
7. 是否把赏金符、智慧符、莲花、肉山、塔钱、击杀、助攻和被动金钱全部拆开，并分别进入路线收益？
8. “漏线”应该按己方兵死亡、玩家未在经验区、队友是否收走、是否因合理团战/目标放弃来区分吗？
9. 叠野收益应归给叠野者、清野者，还是同时输出个人收益和团队创造价值？
10. 前 20 分钟报告最希望优先回答哪三类问题：补刀基本功、线野节奏、危险路线、资源冲突、支援机会成本，还是关键时间点准备？

---

## 17. 建议下一版先修的规则顺序

1. 拆开完整 `gold_reason`，先保证兵线、中立、英雄、助攻、塔、肉山、神符和被动金钱来源正确。
2. 把 `combat_or_idle` 拆成参战、移动、回城/补给、等待刷新、死亡和未知空窗，禁止把未知直接写成梦游。
3. 为战斗加入长片段二次切分，先解决同一场被串联的问题。
4. 将安全兵线从单一风险分改为“兵线存在门槛 + 可达性门槛 + 敌方威胁门槛 + 队友资源归属门槛”。
5. 团战责任结论先经过可施法性、可到场性、目标可救性和英雄职责门禁，再输出问题标签。
6. 最后才做“理论最优打钱路线”，因为它依赖前面所有事实层都可靠。
